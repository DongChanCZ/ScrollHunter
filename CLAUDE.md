# Scroll Hunter — 개발 사양

이 파일은 **AI 코딩 세션용 사양서**입니다. 사람이 읽는 문서는 `README.md`입니다.

값이 바뀌면 **이 파일과 `Docs/10_결정사항_로그.md`를 함께 갱신**하세요.
두 곳이 어긋나면 1차 프로젝트(Soul Wanderer)의 실패가 반복됩니다.

---

## 프로젝트 개요

| 항목 | 값 |
|---|---|
| 프로젝트명 | Scroll Hunter (가칭) |
| 장르 | 실시간 로그라이크 카드 전투 |
| 엔진 | Unity 6.3 LTS (6000.3.18f1), URP |
| 플랫폼 | PC |
| 작업 폴더 | `Assets/_Project/` (Scripts, Data, Prefabs, Scenes, Materials) |
| 기획 문서 | `Docs/` |

이 프로젝트는 2023년 팀 프로젝트 **Soul Wanderer**의 전투 시스템을 1인 재설계한 것입니다.

---

## 현재 단계

> **프로토타입 (1~4단계).** 전투 재미 검증이 목적입니다.
>
> 아트는 전혀 필요 없습니다. 큐브/캡슐과 Unity 기본 UI만 사용합니다.
> 검증이 끝나기 전에는 모델·이펙트·사운드를 넣지 않습니다.

**검증하려는 것:** 적의 캐스팅을 읽고 한정된 코스트를 배분하는 것이 재미있는가.

---

## 전투 구조

- 실시간. 플레이어 1인 vs 적 최대 3체
- 플레이어는 코스트만 충분하면 아무 때나 카드를 쓸 수 있다
- 턴 개념 없음. 행동 게이지(ATB) 없음
- **개별 스킬 쿨타임 없음.** 카드 순환이 그 역할을 한다

### 전투 진행

- 런 1회 = 전투 5회 (잡몹 4 + 보스 1). 선형 진행, 분기 없음
- 전투 종료 시 보상 1회 → 덱 리빌딩 → 다음 전투
- HP는 전투 사이에 이월된다. 방어도는 전투 종료 시 소멸한다

---

## 자원 — 코스트

| 항목 | 값 |
|---|---|
| 초당 충전 | 0.8 |
| 상한 | 10 (초과분 소멸) |
| 전투 시작값 | 3 |

---

## 카드 — 덱과 손패

- 덱 8장(순서 있음) → 앞 4장이 **손패(QWER)** → 나머지 4장이 **대기열**
- 카드 사용 시 그 카드는 **덱 맨 아래로** 이동, 대기열 맨 앞이 빈 슬롯에 진입
- **결정론적 순환.** 셔플 없음. 등록 순서 그대로
- **손패 슬롯 위치는 고정.** 카드가 왼쪽으로 밀리지 않는다
- 손패 교체/버리기 없음 (4일차에 도입 여부 재검토)

---

## 행동 잠금 (GCD) — 중요

```
행동 잠금 시간 = MAX(그 카드의 캐스팅 시간, 최소 GCD 0.4초)
```

- 캐스팅기는 캐스팅 시간 전체가 잠금, 즉발기는 최소 GCD만 적용
- 캐스팅 중 다른 카드 입력은 **버퍼에 저장하지 않고 무시**
- 캐스팅은 **스스로 취소할 수 없다**
- 캐스팅 중 기절당하면 캐스팅 취소, **코스트 반환 없음**
- 포션(Shift)에는 행동 잠금이 적용되지 않는다
- 손패 슬롯 위에 남은 잠금 시간을 소수점 1자리로 표시

---

## 적 캐스팅 3색

| 색 | 의미 | 대응 |
|---|---|---|
| 🟢 Green | 일반 공격 | 불필요 |
| 🟠 Orange | 기절 공격 | **차단 카드**로 캐스팅 취소 |
| 🔴 Red | 대형 피해 | **방어도 카드**로 흡수. 차단 불가 |

- 캐스팅 구간 내라면 **언제 눌러도 유효.** 타이밍 정밀도(밀리초 판정) 요구 없음
- 적끼리 발동이 겹치지 않도록 **최소 0.5초 간격 강제**

---

## 대응 수단

- **차단(Interrupt):** 캐스팅 중인 적 대상. 성공 시 캐스팅 취소 + 1.5초 경직
  - 캐스팅 중이 아닌 적에게는 사용 불가 (코스트 소모 없음)
  - 빨강 캐스팅에는 실패 (코스트는 소모)
- **방어도(Shield):** 플레이어에게 부여. 피해는 방어도 → HP 순으로 차감
  - 합산 누적, 상한 없음. **전투 종료 시 소멸**

---

## 플레이어

| 항목 | 값 |
|---|---|
| HP | 500 |
| 공격력 | 100 |
| 방어력 | 20 |
| 크리티컬률 | 15% |
| 크리티컬 배율 | 1.8 |

- **HP는 전투 사이에 회복되지 않는다** (소모성 자원)
- 회복 포션: **런 전체 3회**, 최대 HP의 50% 회복, 행동 잠금 미적용

```
최종피해 = (공격력 × 계수) × (1 - 방어력/(방어력+100)) × 크리배율
크리배율 = 크리티컬 시 1.8, 아니면 1.0
피해 적용 = 방어도에서 먼저 차감 → 남은 피해를 HP에서 차감
```

---

## 일시정지 / 배속

- 일시정지 중에는 **카드를 사용할 수 없다**
- 일시정지 중 카드·버프·디버프에 마우스오버해 **정보만 확인** 가능
- 1배속 / 2배속 전환 가능. 코스트 회복·캐스팅·도트가 모두 동일 배속

---

## 코딩 규칙 (반드시 지킬 것)

- 모든 수치는 `[SerializeField]` 또는 ScriptableObject로 **인스펙터에 노출.** 하드코딩 금지
- 스킬/적 데이터는 **ScriptableObject로 분리**
- **덱 구성은 외부에서 주입받는 구조**로 작성할 것 (나중에 리빌딩·계승을 붙이기 위해)
- 화면에 표시되는 **텍스트를 코드에 하드코딩하지 말 것** (추후 로컬라이징)
- **입력은 구형 Input API**(`Input.GetKeyDown`)를 사용한다
  - 프로젝트의 Active Input Handling이 `Both`로 설정되어 있다
  - Input System 패키지 방식으로 작성하지 말 것
- API가 Unity 6에서 사용 중단(Obsolete)된 것이라면 **대체 API를 알려줄 것**
- **한 번에 한 기능씩.** 요청하지 않은 기능(사운드, 파티클, 세이브 등)은 만들지 말 것
- 만든 뒤 **"어떻게 실행해서 확인하는지"**를 항상 함께 설명할 것
- 스크립트를 준 뒤 **Unity에서 해야 할 배치 작업**(GameObject 생성, 컴포넌트 부착, 참조 연결)을 순서대로 안내할 것

---

## 아직 구현하지 않는 것

프로토타입 4단계까지는 아래를 만들지 않습니다. 요청받지 않았다면 먼저 제안하지도 마세요.

- 보상 3택1, 덱 리빌딩 화면
- 패시브 시스템
- 계승 시스템, JSON 저장
- 난이도 등급
- 보스, 페이즈 전환
- 상태이상 (기절 제외)
- 사운드, 파티클, 3D 모델, 애니메이션
- 타이틀 화면, 옵션 메뉴

---

## 참고 문서

| 파일 | 내용 |
|---|---|
| `Docs/10_결정사항_로그.md` | **모든 수치의 원본.** 이 파일과 항상 일치시킬 것 |
| `Docs/02_전투시스템_기획서.md` | 전투 상세 사양, 예외 처리 목록 |
| `Docs/03_스킬_기획서.md` | 스킬 14종, 패시브 |
| `Docs/04_적_패턴_기획서.md` | 적 5종 타임라인 |
| `Docs/09_프로토타입_제작절차.md` | 1~4단계 요청 순서 |
| `Docs/11_계승_시스템_기획서.md` | 계승·난이도 (16일차 이후) |
| `Docs/밸런스시트.xlsx` | 수치 시뮬레이션 |

---

## 사양 변경 시

1. **이 파일**을 고친다
2. `Docs/10_결정사항_로그.md`의 해당 값을 고친다
3. 관련 기획서의 「변경 이력」 표에 기록한다
4. `Docs/07_개발일지.md`에 한 줄 남긴다

> 1차 프로젝트가 실패한 지점이 정확히 여기입니다. 사양은 바뀌었는데 문서가 안 따라가서, 최종 빌드를 설명하는 문서가 하나도 남지 않았습니다.
