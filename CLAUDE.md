# Scroll Hunter — 개발 사양

이 파일은 **AI 코딩 세션용 사양서**입니다. 사람이 읽는 문서는 `README.md`입니다.

값이 바뀌면 **이 파일과 `Docs/10_결정사항_로그.md`를 함께 갱신**하세요.
두 곳이 어긋나면 1차 프로젝트(Soul Wanderer)의 실패가 반복됩니다.

---

## 프로젝트 개요

| 항목 | 값 |
|---|---|
| 프로젝트명 | Scroll Hunter (가칭) |
| 장르 | 실시간 로그라이크 카드 전투 |
| 엔진 | Unity 6.3 LTS (6000.3.18f1), URP |
| 플랫폼 | PC |
| 작업 폴더 | `Assets/_Project/` (Scripts, Data, Prefabs, Scenes, Materials) |
| 기획 문서 | `Docs/` |

이 프로젝트는 2023년 팀 프로젝트 **Soul Wanderer**의 전투 시스템을 1인 재설계한 것입니다.

---

## 현재 단계

> **프로토타입 (1~4단계).** 전투 재미 검증이 목적입니다.
>
> 아트는 전혀 필요 없습니다. 큐브/캡슐과 Unity 기본 UI만 사용합니다.
> 검증이 끝나기 전에는 모델·이펙트·사운드를 넣지 않습니다.

**검증하려는 것:** 적의 캐스팅을 읽고 한정된 코스트를 배분하는 것이 재미있는가.

---

## 전투 구조

- 실시간. 플레이어 1인 vs 적 최대 3체
- 플레이어는 코스트만 충분하면 아무 때나 카드를 쓸 수 있다
- 턴 개념 없음. 행동 게이지(ATB) 없음
- **개별 스킬 쿨타임 없음.** 카드 순환이 그 역할을 한다

### 전투 진행

- 런 1회 = 전투 5회 (잡몹 4 + 보스 1). 선형 진행, 분기 없음
- 전투 종료 시 보상 1회 → 덱 리빌딩 → 다음 전투
- HP는 전투 사이에 이월된다. 방어도는 전투 종료 시 소멸한다

---

## 자원 — 코스트

| 항목 | 값 |
|---|---|
| 초당 충전 | 0.8 |
| 상한 | 10 (초과분 소멸) |
| 전투 시작값 | 3 |

---

## 카드 — 덱과 손패

- 덱 8장(순서 있음) → 앞 4장이 **손패(QWER)** → 나머지 4장이 **대기열**
- **유효한 입력 수락 즉시** 코스트를 소모하고 사용 카드를 **덱 맨 아래로** 이동, 대기열 맨 앞이 같은 슬롯에 진입
- 새 카드는 행동 잠금이 끝나야 사용 가능. 거절된 입력은 코스트·카드 소모 없음
- **결정론적 순환.** 셔플 없음. 등록 순서 그대로
- **손패 슬롯 위치는 고정.** 카드가 왼쪽으로 밀리지 않는다
- 카드 사용과 무관한 수동 손패 교체/버리기 없음 (4일차에 도입 여부 재검토)

---

## 행동 잠금 (GCD) — 중요

```
행동 잠금 시간 = MAX(그 카드의 캐스팅 시간, 최소 GCD 0.4초)
```

- 캐스팅기는 캐스팅 시간 전체가 잠금, 즉발기는 최소 GCD만 적용
- **카드 효과는 캐스팅이 끝나야 적용된다.** 누르는 순간이 아니다. 코스트 소모와 카드 순환은 유효 입력 수락 즉시 처리한다
  - 타이머가 둘이다 — 효과까지 `캐스팅 시간`, 다음 입력까지 `MAX(캐스팅 시간, 최소 GCD)`
- 캐스팅 중 다른 카드 입력은 **버퍼에 저장하지 않고 무시**
- 캐스팅은 **스스로 취소할 수 없다**
- 캐스팅 중 기절당하면 캐스팅 취소, **코스트 반환 없음**
  - **카드는 입력 수락 시 이미 순환했으므로 추가 순환하지 않는다.** 효과만 발생하지 않는다
  - 코스트와 카드를 둘 다 잃는 것이 「주황에 대응하지 못한 값」이다
  - 기절이 행동 잠금을 대체한다. 기절이 풀리면 곧바로 쓸 수 있다
- 포션(Shift)에는 행동 잠금이 적용되지 않는다
- 손패 슬롯 위에 남은 잠금 시간을 소수점 1자리로 표시

---

## 적 캐스팅 3색

| 색 | 의미 | 대응 |
|---|---|---|
| 🟢 Green | 일반 공격 | 불필요하나 차단 가능 |
| 🟠 Orange | 기절 공격 | **차단 카드**로 캐스팅 취소 |
| 🔴 Red | 대형 피해 | **방어도 카드**로 흡수. 차단 불가 |

- 입력 시 대상이 캐스팅 중이면 차단 사용 가능. **침묵 시전 0.2초가 끝날 때도 초록·주황 캐스팅 중이어야 성공**
- 시전 중 적 공격이 먼저 발동하면 차단 실패·경직 없음. 코스트·카드는 소모
- 적끼리 발동이 겹치지 않도록 **최소 0.5초 간격 강제**

### 적 공격 구성 (중요)

- **모든 적이 🟢 초록을 보유한다.** 주황·빨강은 그 위에 추가되는 공격이다
- 보유 조합: A `🟢` / B `🟢🟠` / C `🟢🔴` / D `🟢🟠🔴` / 보스 `🟢🟠🔴`
- EnemyData는 색 하나가 아니라 **공격 리스트**를 갖는다

### 적 패턴 쿨다운 — 초록 횟수 기반

> ⚠️ 플레이어 카드에는 쿨타임이 없다. 이것은 **적 전용** 규칙이며 이름도 「패턴 쿨다운」으로 구분한다.

| 공격 | 쿨다운 | 우선순위 |
|---|---|---|
| 🟢 | 없음 | 3 (최하) |
| 🟠 | 사용 후 **초록 1회** 경과 | 2 |
| 🔴 | 사용 후 **초록 2회** 경과 | 1 (최상) |

**행동 선택 규칙 (이 순서대로 판정)**

1. 직전 행동이 🟠 또는 🔴였다면 → **반드시 🟢 사용**
2. 쿨다운이 끝난 상위 공격 중 우선순위가 가장 높은 것 사용
3. 없으면 🟢 사용

D형 순환 결과: `🔴 → 🟢 → 🟠 → 🟢 → 🔴 → …` (별도 스케줄링 로직 불필요)

**차단당했을 때도 쿨다운은 소모된다.** 차단당한 상위 공격은 「한 번 쓴 것」으로 친다.
경직이 풀리면 반드시 초록이 먼저 오므로 `🟠(차단) → 🟢 → 🟠` 이 된다.
단 **초록을 차단했을 때는 초록 횟수를 올리지 않는다** — 플레이어가 화면에서 세는 값이기 때문.

### 발동 후 경직

| 공격 | 경직 |
|---|---|
| 🟢 | 1.0초 |
| 🟠 | 1.5초 |
| 🔴 | 2.0초 |
| 차단 성공 시 | **2.5초** |

경직 시간 = 발동부터 다음 캐스팅 시작까지의 간격. 차단 성공 경직은 차단 성공 시점부터 센다.
**첫 캐스팅은 전투 시작과 동시에 시작한다** (시작 지연 0초, `10` N11).

> **이 표가 구현과 일치한다 (2026-09-17 실측 확인).** `10` N6, `04` §3, `08`·`09` 반복 절차의 구표기를 문서 정정 완료했다. 아래 실측 기록 참조.

---

## 대응 수단

- **차단(Interrupt):** 캐스팅 중인 적 대상. 침묵 **2코스트 / 시전 0.2초**. 성공 시 캐스팅 취소 + **2.5초 경직** (위 「발동 후 경직」 표)
  - 캐스팅 중이 아닌 적에게는 사용 불가 (코스트·카드 소모 없음)
  - 초록·주황 캐스팅은 효과 적용 시에도 진행 중이면 성공. 빨강에는 실패 (코스트·카드 소모)
  - 차단 경직 2.5초와 별개로, 적 주황 공격이 플레이어에게 주는 기절은 **2초 유지**
- **방어도(Shield):** 플레이어에게 부여. 피해는 방어도 → HP 순으로 차감
  - 합산 누적, 상한 없음. **전투 종료 시 소멸**

---

## 타겟팅

| 분류 | 대상 | 지정 방식 |
|---|---|---|
| 딜 / 단일 | 타겟 1체 | 마우스 클릭 또는 좌우 방향키 |
| 딜 / 광역 | **적 전체** | 지정 불필요 |
| 차단 | 타겟 1체 | 위와 동일 |
| 방어 | 자신 | 지정 불필요 |

- 타겟 개념은 **하나뿐이다.** 차단 전용 타겟이나 순환 키를 만들지 말 것
- 전투 시작 시 1번 적 자동 지정
- 타겟이 죽으면 자동으로 다음 생존 적으로 이동
- 타겟 위에 조준 마커 표시 (맥동 효과)
- 방향키 이동 순서는 화면 배치 좌→우, 죽은 적은 건너뜀

### 손패 슬롯 비활성 처리

- **타겟이 캐스팅 중이 아니면 차단 카드 슬롯 비활성** (입력 자체가 불가)
- 타겟이 🟠 주황 캐스팅 중 → 활성, 성공
- 타겟이 🔴 빨강 캐스팅 중 → **활성, 실패, 코스트 소모** (의도된 동작)
- 비활성 사유 4가지를 **시각적으로 구분할 것.** 전부 회색이면 원인을 모른다

| 사유 | 적용 범위 | 표시 |
|---|---|---|
| 코스트 부족 | 해당 슬롯만 | 코스트 숫자를 빨갛게 |
| 타겟이 캐스팅 중 아님 | **차단 카드만** | 자물쇠 아이콘 또는 점선 테두리 |
| 행동 잠금 중 | 손패 전체 | **회색** + 남은 시간 숫자 |
| 기절 중 | 손패 전체 | **보라 계열** + 남은 시간 숫자 + **화면 중앙에 기절 표시** |

> 행동 잠금과 기절은 둘 다 「손패 전체 + 남은 시간」이라 반드시 구분해야 한다.
> **슬롯 색 + 화면 중앙 표시를 둘 다 적용한다.** 색은 원인을, 중앙 표시는 상태를 알린다.
> 기절은 2차 작업(카드 효과 지연 적용)과 함께 구현한다.

---

## 단일 / 광역 밸런스

- 손익분기 **적 2체.** 같은 코스트 기준 광역 피해량 = 단일 피해량 × 0.5
- 광역기의 대가는 코스트가 아니라 **캐스팅 시간** (0.8~1.2초)
- 광역기 코스트를 올리면 피해량도 비례해 올릴 것

---

## 플레이어

| 항목 | 값 |
|---|---|
| HP | 500 |
| **공격력** | **없음** (스탯 자체가 존재하지 않음) |
| 방어력 | 20 |
| 크리티컬률 | 15% |
| 크리티컬 배율 | 1.8 |

- **HP는 전투 사이에 회복되지 않는다** (소모성 자원)
- 회복 포션: **런 전체 3회**, 최대 HP의 50% 회복, 행동 잠금 미적용

**플레이어에게 공격력 스탯은 없다.** 피해는 스킬에 적힌 피해량(절대값)으로만 결정된다.
SkillData에 계수(coefficient)가 아니라 **damage(int)** 필드를 둘 것.

```csharp
float raw = damage * hitCount * 100f / (targetDefense + 100f) * critMultiplier;
if (raw <= 0f) return 0;
return Mathf.Max(1, Mathf.FloorToInt(raw + 0.5f));

// 크리배율 = 크리티컬 시 1.8f, 아니면 1.0f
// 피해 적용 = 방어도에서 먼저 차감 → 남은 피해를 HP에서 차감
```

### 수치 타입 규칙 (중요)

- `SkillData.damage`, `EnemyData.damage` 는 **int** (카드·UI에 표시되는 값)
- 계산 중간값은 **float**
- 정수화는 **최종 적용 시 한 번만.** 중간에 여러 번 자르지 말 것
- **곱셈을 먼저** 할 것. `damage * (1 - def/(def+100))` 형태는 부동소수점 오차로
  30 → 24.999998 이 되어 내림 시 24가 된다. `damage * 100 / (def + 100)` 은 정확히 25
- 반올림은 **`Mathf.FloorToInt(raw + 0.5f)`** 사용
  - ⚠️ `Mathf.RoundToInt`와 `Math.Round`는 **둘 다 은행가 반올림**이다 (0.5→0, 2.5→2). 쓰지 말 것
  - 검증: 0.5→1, 1.5→2, 2.5→3, 3.5→4
- 최소 피해 **1**, 단 **피해량 0인 카드(방어·차단)는 0을 유지**할 것
- 검증값 (플레이어 방어력 20): 피해량 30 → **25**, 40 → **33**, 150 → **125**

---

## 일시정지 / 배속

- 일시정지 중에는 **카드를 사용할 수 없다**
- 일시정지 중 카드·버프·디버프에 마우스오버해 **정보만 확인** 가능
- 1배속 / 2배속 전환 가능. 코스트 회복·캐스팅·도트가 모두 동일 배속

---

## 코딩 규칙 (반드시 지킬 것)

- 모든 수치는 `[SerializeField]` 또는 ScriptableObject로 **인스펙터에 노출.** 하드코딩 금지
- 스킬/적 데이터는 **ScriptableObject로 분리**
- **덱 구성은 외부에서 주입받는 구조**로 작성할 것 (나중에 리빌딩·계승을 붙이기 위해)
- 화면에 표시되는 **텍스트를 코드에 하드코딩하지 말 것** (추후 로컬라이징)
- **입력은 구형 Input API**(`Input.GetKeyDown`)를 사용한다
  - 프로젝트의 Active Input Handling이 `Both`로 설정되어 있다
  - Input System 패키지 방식으로 작성하지 말 것
- API가 Unity 6에서 사용 중단(Obsolete)된 것이라면 **대체 API를 알려줄 것**
- **한 번에 한 기능씩.** 요청하지 않은 기능(사운드, 파티클, 세이브 등)은 만들지 말 것
- 만든 뒤 **"어떻게 실행해서 확인하는지"**를 항상 함께 설명할 것
- 스크립트를 준 뒤 **Unity에서 해야 할 배치 작업**(GameObject 생성, 컴포넌트 부착, 참조 연결)을 순서대로 안내할 것

---

## 아직 구현하지 않는 것

프로토타입 4단계까지는 아래를 만들지 않습니다. 요청받지 않았다면 먼저 제안하지도 마세요.

- 보상 3택1, 덱 리빌딩 화면
- 패시브 시스템
- 계승 시스템, JSON 저장
- 난이도 등급
- 보스, 페이즈 전환
- 상태이상 (기절 제외)
- 사운드, 파티클, 3D 모델, 애니메이션
- 타이틀 화면, 옵션 메뉴

---

## 참고 문서

| 파일 | 내용 |
|---|---|
| `Docs/10_결정사항_로그.md` | **모든 수치의 원본.** 이 파일과 항상 일치시킬 것 |
| `Docs/02_전투시스템_기획서.md` | 전투 상세 사양, 예외 처리 목록 |
| `Docs/03_스킬_기획서.md` | 스킬 14종, 패시브 |
| `Docs/04_적_패턴_기획서.md` | 적 5종 타임라인 |
| `Docs/09_프로토타입_제작절차.md` | 1~4단계 요청 순서 |
| `Docs/11_계승_시스템_기획서.md` | 계승·난이도 (**일정 미정** — `07` D-11 기능 동결과 충돌, 2026-09-17) |
| `Docs/밸런스시트.xlsx` | 수치 시뮬레이션 |

---

## 사양 변경 시

1. **이 파일과 `AGENTS.md`를 함께** 고친다 — 두 파일은 내용이 같아야 한다
   Claude는 `CLAUDE.md`를, Codex는 `AGENTS.md`를 자동으로 읽는다. 한쪽만 고치면 두 AI의 사양이 갈린다
2. `Docs/10_결정사항_로그.md`의 해당 값을 고친다
3. 관련 기획서의 「변경 이력」 표에 **한 줄** 기록한다
   > ⚠️ **문서 말미에 사양 전문을 복사해 붙이지 말 것.** 값이 바뀔 때 여러 문서를 동시에 고쳐야 해서
   > 드리프트가 생긴다. 사양 본문은 `Docs/10`에 한 벌만 두고, 나머지 문서는 변경 이력 한 줄에서 `10`을 가리킨다.
   > 그 문서 고유의 확인 절차·실측 기록만 해당 문서에 남긴다
4. `Docs/07_개발일지.md`의 **그날 `D-NN` 항목 안에** 한 줄 남긴다 (문서 끝에 새 절을 만들지 말 것)

> 1차 프로젝트가 실패한 지점이 정확히 여기입니다. 사양은 바뀌었는데 문서가 안 따라가서, 최종 빌드를 설명하는 문서가 하나도 남지 않았습니다.


## 공동 문서 작업 범위 (2026-09-16 사용자 지정)

- 기획 문서 작업은 `Docs/`에서 수행한다. **프로젝트 루트의 `AGENTS.md`와 `CLAUDE.md`는 예외적으로 수정 가능**하며, 사양 변경 시 `Docs/10_결정사항_로그.md`와 함께 동기화한다.
- 그 외 Docs 밖 프로젝트 파일은 기획 문서 작업에서 읽기만 한다. 실제 세션 쓰기 권한은 별도로 확인한다.
- Claude와 같은 파일의 동시 편집을 피하고, 검증할 때 저장된 최신 문서를 다시 읽는다.
- 2026-09-16 문서 동기화: 초록 차단 허용, 침묵 2코스트·성공 경직 2.5초, 효과 적용 시 차단 판정, 입력 수락 즉시 카드 순환. **구현 반영·실행 검증 완료.**
- 2026-09-17: **사양 기록을 원본 한 곳으로 모았다.** 2026-09-16 차단·손패 블록과 2026-09-17 적 행동 간격 블록이
  각각 여러 문서에 전문 복제돼 있던 것을 정리했다. **원본은 `Docs/10`의 해당 날짜 절**이고, `02`·`03`·`04` 는 각자의
  변경 이력에 소관 내용을, `08`·`09`·`12` 는 포인터만 둔다. `09` 의 「새 사양 확인 순서」와 `07` D-05 의 실측 기록은
  문서 고유분이라 남겼다. 앞으로는 위 「사양 변경 시」 3번을 따를 것.
- 2026-09-17: **4단계 계측 검증·비교 시험 11회·1차 해석 완료.** 상세·실측·미해결 쟁점은 `Docs/07` D-05 참조. 현재 결론은 수치 유지와 적 구성만 바꾸는 추가 시험 제안이며, 반복 플레이의 재미와 유일 전략 여부는 미확인이다. 당시 제안 후 임시 순환 및 적 구성별 시험을 수행했다. 최신 진행은 D-06 참조.
- 2026-09-18: **임시 순환 3회·B 단독 3회·C 단독 2회·B+C 2회 계측 완료.** 설정·원복값은 `Docs/07` D-05, 구성별 결과·정성 관찰·미확인 사항·다음 행동은 D-06 참조. 정식 채택·임시값 원복은 미결정이며 `10`의 확정 수치는 유지한다. 적 구성은 3단계 Claude 실행 기록상 복원 완료(독립 실행 검증 아님). BC-2 원문 대조·요약 정정 완료, 회차 집계 변경 없음. 플레이 의도 보완이 남아 있다. 3단계 요약의 집계·해석 정정은 07 D-06 참조. 전문 중복 기록·자동 사양 변경 금지.
- 2026-09-18: **손패 카드 유형 표시 추가 (식별성 개선, 수치 변경 없음).** BC-2 침묵 오인 사용(`Docs/260918_3단계.md` 추가 플레이어 답변 Q4) 대응으로 `CombatUI.cs` 손패 슬롯에 유형 도형+짧은 이름을 추가했다 — 공격(파랑 사각) · 방어(청록 원, Unity 내장 Knob) · 차단(자홍 마름모, 사각을 45도 회전). 색은 적 캐스팅 초록·주황·빨강, 슬롯 상태색(잠금 회색·기절 보라)과 겹치지 않게 골랐다. `SkillData.Category`는 기존 값이라 카드·적 수치, 판정·계측 로직, 씬 적 구성은 손대지 않았고 이름·코스트·단축키 표시도 그대로다. Play 모드 스크린샷으로 확인: 최초 손패(공격2·방어1·차단1) 표시 일치, 카드 순환(수정방패→폭발) 시 유형 즉시 갱신, 코스트부족·행동잠금·기절·차단 대상 없음(잠김) 4개 상태 모두에서 유형·이름·코스트 동시 가독. 상태는 리플렉션으로 강제해 확인했고 실제 마우스·키보드 조작 테스트는 미수행이며, 이번 확인은 식별성 개선 여부이지 재미·밸런스 검증이 아니다. 후속 조정: 행동 잠금·기절 배경(회색·보라)은 그대로 두고 이름·유형·코스트·단축키·남은 시간 글자색을 전부 흰색으로 올려 대비를 높였다(`lockedOrStunnedTextColor`, `CombatUI.cs`). `GmarketSansMedium SDF.asset`이 함께 바뀌는데, '차단' 두 글자가 이 폰트에서 처음 쓰이며 TMP가 동적으로 글리프 2개를 추가한 것뿐이라 새 폰트 도입이 아니다.
- 2026-09-18: **플레이어 시전 표시 추가 (신규 UI, 수치·판정 변경 없음).** 손패 위(Canvas 직속 `CastBar`, 화면 x 420~1260 / y 290~362)에 시전 중인 스킬 이름·남은 시간·진행 바를 띄운다. 표시 기준은 손패 슬롯이 아니라 `DeckSystem.CastingCard`다 — 카드는 입력 수락 즉시 순환하므로(`10` A14) 슬롯을 읽으면 다른 스킬이 뜬다. 진행률 분모는 그 카드의 `CastTime`, 남은 시간은 `CastRemaining`만 쓰고, 행동 잠금 MAX(캐스팅, 최소 GCD 0.4)(`10` A1)은 손패 슬롯 숫자로 그대로 두어 두 타이머를 섞지 않는다. 효과 적용 시(= 캐스팅 완료, `10` A11) 표시를 끄고, 기절 취소(`10` A12 — 코스트 반환·추가 순환 없음)일 때만 짧게 「… 시전 취소」를 남긴다. 완료가 곧 성공은 아니므로(`10` T10 빨강 대상 차단) 완료 표시는 성공을 단정하지 않는다. 전투 종료는 `CombatMetrics.Ended`로 감지해 숨긴다 — 패배는 `timeScale=0`이라 그냥 두면 시전 바가 얼어붙은 채 남는다. 코드는 `DeckSystem`에 읽기 전용 접근자 4개와 종료 사유 기록 한 줄, `CombatUI`에 표시 로직만 추가했고 코스트·순환·판정·실행 순서는 무변경. 확인: 짧은 시전(0.3초)·긴 시전(1.0초) 이름·시간·진행률 일치, 순환 후에도 진행 중 스킬명 불변, 정상 완료와 기절 취소 구분, 취소 시 코스트·손패 되돌림 없음, 패배 시 표시 사라짐, 실시간 입력 9.35s→효과 9.68s. 미확인: 실제 키보드 조작·일시정지·2배속·승리 직후 시전 중이던 경우. 후속 사용자 요청으로 `02` §3의 순환 구표기를 `10` A14에 맞게 정정하고, `02` UI 목록·`12` 인계에 시전 표시 참조를 반영했다. Codex 실행 확인 범위는 `Docs/260918_UI_실행확인.md` 참조. 당시 후속 2·3단계는 보류 상태였으며 이후 정보 확인 2단계 구현은 다음 기록 참조. 광역 3단계는 계속 보류 중이다. UI가 바뀌었으므로 이 뒤의 계측은 이전 회차와 같은 묶음으로 합치지 말 것. `GmarketSansMedium SDF.asset`은 '시·전·취·소' 글리프가 TMP로 동적 추가된 것뿐이다.
- 2026-09-18: **후속 2단계 카드·적 공격 정보/일시정지 구현 완료(Codex).** 사용자 요청으로 코드·씬 수정 범위를 허용받아 진행. `CombatInfoUI.cs` 신설 및 Battle 씬 Canvas/CombatInformation 연결. 손패 hover 정보, 손패 우클릭 정지·재우클릭 재개, 정지 버튼, 현재 타겟 공격 정보. 현재 SkillData/EnemyAttack와 DeckSystem.InterruptStagger를 읽어 임시 수치와 표시가 어긋나지 않게 했다. DeckSystem은 정지·종료 시 직접 사용 거절, Enemy는 정지·종료 공격 중단, EnemyManager는 UI 클릭 관통 차단을 추가했다. 기존 TMP 동적 폰트도 글리프 생성으로 변경됨. 임시 cost·damage·덱·적 구성 유지. 기능 함수 호출로 정지 중 시전/GCD/자원/적/기절 정지, 재개, 타겟 변경·강제 사망, 강제 승패 뒤 재개·사용 차단 확인. 실제 마우스/키보드 및 다른 화면비 미확인. 2배속·포션·키워드 중첩 팝업 미구현, 3단계 광역은 보류. 배치·조작·변경 파일·검증 한계는 `Docs/260918_정보확인_2단계_구현검증.md` 참조.
- 2026-09-18: **손패 좌클릭 사용 추가(Codex, 사용자 요청).** CombatInfoUI.Update에서 슬롯 좌클릭을 QWER과 같은 DeckSystem.TryUseSlot에 연결. 사양 원본은 Docs/10 A17. 정지 버튼 우선 처리, 우클릭 정지·재개 유지. 카드 수치·씬 배치 변경 없음. Play 함수 호출로 코스트3→1·순환·시전 시작 및 잠금/정지/기절 거절 확인. 실제 마우스 경로는 미확인. 기존 사용 로그 QWER은 슬롯 이름이며 입력 장치를 뜻하지 않음. 상세는 Docs/260918_정보확인_2단계_구현검증.md 후속 추가 참조.
- 2026-09-18: **후속 UI·입력 문서 동기화(Codex).** Docs/01·02·04·07·08·09·10·12 정정: 일시정지 채택·대상 정보창 상태·좌클릭 인계 및 계측 용어 반영, 적별 1색·후속 단계 보류는 당시 기록으로 구분. 원본은 10 A6·A17·M항, 변경 요약은 07 D-06. 코드·씬·수치·원시 로그 변경 없음. 실제 마우스 입력 검증은 미완료 유지.
- 첫 캐스팅 시작 시점은 `10` N11 참조. 2026-09-17 해석 정정: 회복량 적정·고코스트 카드 무가치·무료 손패 교체를 단정하지 않는다. 게임 수치·사양 변경 없음.

## 2026-09-17 적 행동 간격 실측 — 문서 정정 완료

**구현이 맞고 문서가 틀렸다. 코드·씬·에셋을 문서에 맞추지 말 것.**
에셋 `staggerAfterCast` 가 초록 1 / 주황 1.5 / 빨강 2 로 저장돼 있어 위 「발동 후 경직」 표(M8)와 일치한다.

실측 (무입력, 전투 경과 초, 관측기로 프레임 단위 기록)

| 적 | 발동 | 다음 캐스팅 시작 | 간격 | 목표 |
|---|---|---|---|---|
| A 🟢 | 2.500 | 3.502 | 1.002 | 1.0 |
| B 🟠 | 3.003 | 4.505 | **1.502** | 1.5 |
| C 🔴 | 4.001 | 6.004 | **2.003** | 2.0 |

- 코드 기본값 `EnemyAttack.staggerAfterCast = 1f` 는 **인스펙터에서 리스트에 새 항목을 추가할 때만** 쓰인다. 에셋에서 역직렬화된 값이 최종값이다
- 다음 캐스팅 시작 = **실제 발동 시각 + 그 공격의 경직.** 별도 「행동 간격」 필드도, 추가 대기도 없다 (`Enemy.Fire` 가 `restTimer` 를 시작한다)
- 적끼리 최소 발동 간격 0.5초로 발동이 밀리면 경직도 **밀린 발동 시각부터** 센다. 체인 전체가 함께 밀린다 (실측: C 빨강 캐스팅 완료 17.009 → 발동 17.100, 0.091초 지연)
- `04` §3 의 「경직 해제」(3.0 / 3.5 / 4.8s)는 구현에 대응하는 개념이 없다

**문서 정정 완료 (2026-09-17).** `10` N6을 M8 참조로, `04` B·C 다음 시전을 4.5초·6.0초로 정정했다. 별도 경직 해제 시각과 하위 구간 설명을 삭제하고, `08`·`09`의 일괄 1초 휴식 지시도 색별 경직으로 동기화했다. `07`에 기록했다. M8·코드·씬·에셋 변경 없음.

**무대응 전멸 시각은 고정값이 아니다 (2026-09-17 확인).** 같은 빌드에서 세 번 재서 **17.01 / 17.05 / 17.10초**가 나왔다.
마지막 타격이 프레임 경계에 걸리고 최소 발동 간격 0.5초 충돌 여부가 프레임 페이싱에 따라 갈리기 때문이다.
`10` N10의 17.01초는 그 구간의 한 표본이며, **비교 시험에서 ±0.1초 차이는 유의미한 신호로 읽지 말 것.** 전멸 기준값 자체는 변경하지 않았다.


## 2026-09-17 판정 기준 문서 인계

- 2026-09-17: `Docs/08`의 「4일차 판단 기준」을 최신 `09` 4단계 ⑥과 동기화했다. 대응 횟수만으로 가설 실패를 단정하거나 즉시 수치를 조절하지 않고, 같은 조건에서 승패·처치·HP·사용 기회와 관찰 메모를 함께 평가한다. 수치·코드 변경 없음. `01` S1~S5도 후속 정정 완료했다(아래 인계 참조).


## 2026-09-17 성공 기준 문서 인계

- 2026-09-17: `01` §6 S1~S5를 `09` 4단계 ⑥과 동기화했다. 횟수·순서 변화·변경 이력 건수만으로 성공을 판정하지 않고, 위협에 따른 선택·플레이 의도·승패/처치/HP·문서와 실제 구현의 대조 근거를 평가한다. 기존 전투 시간 목표는 유지하되 단독 합격선으로 사용하지 않는다. 당시 기준 정정 이후 1차 비교 시험 결과가 기입됐다. 최신 결과는 `07` D-05 참조. 수치·코드 변경 없음.
