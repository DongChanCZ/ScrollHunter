# 09. 프로토타입 제작 절차 (4일)

> **Scroll Hunter** 전투 프로토타입. 목표는 하나 — **적의 캐스팅을 읽고 코스트를 배분하는 것이 재미있는가.**
> 이 문서는 "내가 직접 하는 일"과 "AI에게 시키는 일"을 구분해 순서대로 정리한 것입니다.
> 코드 세션에서는 `08_코드세션_인계사양`의 사양 블록을 먼저 붙인 뒤 여기 프롬프트를 사용하세요.

---

> **후속 UI·입력 작업(2026-09-18):** 아래 1~4단계는 초기 프로토타입 제작 순서다. 플레이어 시전 표시·정보 확인·일시정지·좌클릭 사용의 최신 구현 범위는 `260918_UI_실행확인.md`와 `260918_정보확인_2단계_구현검증.md` 참조. UI 변경 뒤 시험은 이전 계측과 구분한다.
> **실제 입력 확인은 미완료:** 사용 가능한 손패 좌클릭 시 1회 사용·코스트 소모·같은 슬롯 순환, QWER과 동일 판정, 잠금·기절·정지·종료 중 사용 거절, 우클릭 정지·재개 유지, 카드 클릭 시 타겟 변경 없음 여부를 확인한다. 코스트 부족 입력도 좌클릭으로 1회 집계되는지 확인한다. 로그의 QWER 표기는 슬롯 이름이며 입력 장치 식별값이 아니다.

## 전체 일정

| 단계 | 내용 | 소요 | 결과물 |
|---|---|---|---|
| 0 | 프로젝트 준비 (AI 없이 혼자) | 1시간 | 빈 씬 + 폴더 구조 |
| 1 | 코스트 + 손패 순환 | 반나절 | 적 없이 카드만 돌아감 |
| 2 | 적과 캐스팅 바 | 반나절 | 맞기만 하는 상태 |
| 3 | 차단 + 방어도 | 1일 | **핵심 재미 검증 가능** |
| 4 | 계측 로그 | 반나절 | 숫자로 판단 |
| — | 판정 | — | 계속 갈지 방향을 틀지 결정 |

---

## 0단계 — 프로젝트 준비 (혼자, 프롬프트 없음)

<!-- AI에게 시킬 수 없는 부분입니다. 먼저 해두지 않으면 1단계에서 막힙니다. -->

### 내가 하는 일

| # | 작업 | 비고 |
|---|---|---|
| 1 | Unity Hub → **6.3 LTS (6000.3.18f1)** | 이미 설치되어 있음 |
| 2 | 프로젝트 생성: `ScrollHunter` | **Universal 3D** 템플릿. 경로에 한글·공백 없게 |
| 3 | **Active Input Handling을 `Both`로 변경** ★ | Edit → Project Settings → Player → Other Settings. **에디터 재시작 필요** |
| 4 | 폴더 생성: `Assets/_Project/` 아래 `Scripts` `Data` `Prefabs` `Scenes` `Materials` | 언더바로 시작하면 목록 최상단에 고정됨 |
| 5 | 씬 생성: `Scenes/Battle.unity` | 저장해두기 |
| 6 | **TextMeshPro 리소스 임포트** | Window → TextMeshPro → Import TMP Essential Resources. 메뉴가 없으면 TMP 텍스트를 처음 만들 때 팝업이 뜸 |
| 7 | Canvas 생성 | UI → Canvas, Canvas Scaler를 **Scale With Screen Size / 1920×1080** |
| 8 | **Git 초기화 또는 폴더 백업** | 단계마다 백업. 안 하면 되돌릴 수 없습니다 |

### 3번이 가장 중요합니다 ★

Unity 6의 Universal 3D 템플릿은 **Input System 패키지**를 기본으로 씁니다.
이 상태에서 구형 `Input.GetKeyDown(KeyCode.Q)` 코드를 실행하면 **런타임 예외**가 납니다.
QWER 등 구형 Input API를 쓰는 카드 입력이 이 설정에 의존하므로 1단계부터 막힙니다. 후속 손패 좌클릭 사용 사양은 `10` A17 참조.

| 선택지 | 내용 | 권장 |
|---|---|---|
| **Both** | 구형 Input API를 그대로 사용 가능 | ✅ 4일 프로토타입에는 이쪽 |
| Input System Package (New) | InputAction 에셋 설정 작업이 추가됨 | 정석이지만 느림 |

> `Both`로 두면 인계 사양의 "구형 Input API 사용" 규칙과 맞물려 AI가 헤매지 않습니다.
> Input System으로 가실 거라면 인계 사양의 해당 줄을 고치고, 프롬프트에 "InputAction으로 QWER를 바인딩해서 작성해줘"를 명시하세요.

### 확인

- 씬을 실행(▶)했을 때 에러 없이 빈 화면이 뜨면 준비 완료
- Project Settings에서 Active Input Handling이 `Both`인지 재확인

> **8번을 꼭 하세요.** 3단계에서 코드가 꼬였을 때 되돌아갈 지점이 없으면 하루가 날아갑니다.
> Git이 익숙하지 않으면 매 단계 시작 전에 프로젝트 폴더를 통째로 복사해 `ScrollHunter_D1` 식으로 두세요.

---

## 1단계 — 코스트 + 손패 순환 (반나절)

### ① 프롬프트

```
1단계만 만들어줘. 적은 아직 없어도 된다.

1. CostSystem: 초당 0.8 충전, 상한 10, 시작 3
   - 현재 코스트를 화면 좌하단에 숫자로 표시 (소수점 1자리)
2. SkillData (ScriptableObject): 이름, 코스트, 분류(Deal/Interrupt/Shield), 피해량(int), 타수, 캐스팅 시간
   - CreateAssetMenu로 에디터에서 생성 가능하게
3. DeckSystem: SkillData 8개 리스트를 받아 앞 4장을 손패, 나머지 4장을 대기열로 관리
   - QWER 키로 손패 사용
   - 코스트 부족 시 사용 실패 + 콘솔 로그
   - 유효 입력 수락 즉시 사용한 카드는 덱 맨 아래로, 대기열 맨 앞이 같은 슬롯에 진입. 기절 취소 시 추가 순환 없음
   - 슬롯 위치는 고정 (카드가 왼쪽으로 밀리지 않음)
4. 행동 잠금(GCD): 카드 사용 후 MAX(캐스팅 시간, 최소 GCD 0.4초) 동안 입력 무시
   - 최소 GCD(0.4)는 [SerializeField]로 노출
   - 잠금 중 손패 슬롯을 회색 처리하고 남은 시간을 소수점 1자리로 표시
   - 캐스팅 중 다른 카드 입력은 버퍼에 저장하지 말고 무시
5. UI: 손패 4칸에 카드 이름과 코스트 표시. 옆에 대기열 다음 1장을 회색으로 표시

만든 뒤, 내가 Unity에서 해야 할 작업을 순서대로 알려줘.
어떤 GameObject를 만들고 어떤 컴포넌트를 붙이고 무엇을 연결해야 하는지 구체적으로.
```

### ② 받은 뒤 내가 하는 일

| # | 작업 |
|---|---|
| 1 | 스크립트를 `_Project/Scripts/`에 `.cs` 파일로 저장 (클래스명 = 파일명) |
| 2 | 콘솔에 컴파일 에러가 없는지 확인 |
| 3 | `Data/` 폴더에서 우클릭 → Create → SkillData로 **테스트 카드 8개 생성** |
| 4 | 각 카드에 이름·코스트·분류 입력 (아래 표 참고) |
| 5 | 빈 GameObject `GameManager` 생성 → CostSystem, DeckSystem 컴포넌트 부착 |
| 6 | DeckSystem의 덱 리스트에 카드 8개를 순서대로 드래그 |
| 7 | Canvas에 손패 슬롯 4개 + 대기열 1개 + 코스트 텍스트 배치 후 연결 |

**테스트 카드 8장 (이 값으로 시작)**

| 슬롯 | 이름 | 분류 | 대상 | 코스트 | 피해량 | 타수 | 캐스팅 | 잠금 |
|---|---|---|---|---|---|---|---|---|
| 1 | 화염탄 | Deal | 단일 | 3 | 100 | 1 | 0.3 | 0.4 |
| 2 | 서리화살 | Deal | 단일 | 4 | 120 | 1 | 0.3 | 0.4 |
| 3 | 수정방패 | Shield | 자신 | 3 | — | — | 0.2 | 0.4 |
| 4 | 침묵 | Interrupt | 단일 | 2 | — | — | 0.2 | 0.4 |
| 5 | 연쇄번개 | Deal | 단일 | 6 | 50 | 3 | 1.0 | 1.0 |
| 6 | 폭발 | Deal | 단일 | 5 | 110 | 1 | 0.8 | 0.8 |
| 7 | 관통 | Deal | 단일 | 5 | 150 | 1 | 0.6 | 0.6 |
| 8 | 불기둥 | Deal | 단일 | 7 | 90 | 2 | 1.2 | 1.2 |

> **피해량은 기존 계수 × 100으로 환산한 값입니다.** 공격력 100이 곱해지던 것과 결과가 같으므로 밸런스 변화가 없습니다.
> 광역 카드는 아직 없습니다. 5~8일차에 스킬 14종을 확정할 때 딜 8장을 단일 4 / 광역 4로 나누세요.

> 「잠금」은 `MAX(캐스팅, 최소 GCD 0.4)`으로 자동 계산됩니다. 입력할 필요 없이 확인용입니다.
> **대응 카드(수정방패·침묵)를 3·4번 슬롯에 배치**했습니다. 킵 플레이를 전제로 한 배치입니다.

> 덱 총 코스트·회복량 기준 순환 계산은 **10의 2026-09-16 절**을 참조하세요. 밸런스시트 계산을 실제 플레이 검증으로 해석하지 않습니다.
> 대응 카드가 손패(3·4번 슬롯)에 있으므로 **킵이 가능합니다.** 쓰지 않으면 계속 남습니다.
> 대신 실질 손패가 2칸이 되어, 매 순간 고를 수 있는 딜 카드가 줄어듭니다. 이것이 킵의 대가입니다.
>
> 방어 카드를 한 번 쓰면 덱 맨 아래(8번)로 가고, 다시 손에 오려면 앞 4장을 더 써야 합니다.
> 재등장 시간은 추가로 쓰는 4장의 코스트·잠금·현재 보유 코스트에 따라 달라집니다. 전투당 방어 사용 횟수는 고정하지 않고 4단계에서 측정합니다.
> 즉 "지금 이 빨강에 방어를 쓸 것인가"가 무게 있는 결정이 됩니다.

### ③ 확인 방법

- ▶ 실행 → 코스트가 3에서 올라가는가
- Q를 누르면 화염탄이 사라지고 대기열의 연쇄번개가 입력 수락 즉시 1번 슬롯에 들어오는가
- 코스트가 부족할 때 키를 누르면 실패 로그가 뜨는가
- 사용한 카드가 추가 카드 사용 4회 후 손패에 돌아오는가 (잠금 종료·사용 코스트 확보는 별도)
- **불기둥(캐스팅 1.2초)을 쓴 직후 1.2초 동안 다른 키가 안 먹는가**
- **화염탄(즉발)을 쓴 직후에는 0.4초만 잠기는가**
- **코스트를 10까지 모은 뒤 저가 카드 3장을 약 1.2초에 연속으로 쓸 수 있는가**

---

## 2단계 — 적과 캐스팅 바 (반나절)

### ① 프롬프트

```
2단계. 적을 추가한다.

1. EnemyData (ScriptableObject): 이름, HP, 방어력, 캐스팅 시간, 담당 색상(Green/Orange/Red), 피해량
   - 공격력 필드는 두지 말 것
2. Enemy: 캐스팅 시간만큼 대기 → 발동 허가 후 실제 발동 → 해당 공격의 경직(초록 1.0초 / 주황 1.5초 / 빨강 2.0초) → 반복. 경직은 실제 발동부터 계산하며 별도 추가 대기 없음
   - 적 본체는 Capsule 하나면 충분
3. 캐스팅 바 UI: 적 위에 진행률 바. 담당 색상으로 채워짐
   - 3색은 인스펙터에서 Color로 지정 가능하게
   - 캐스팅 중인 스킬 이름도 바 위에 표시
4. EnemyManager: 적 3체를 관리. 적끼리 발동이 겹치지 않게 최소 0.5초 간격 강제
5. Player: HP 500, 방어력 20. 적 공격 발동 시 피해 계산 후 적용
   - 피해 = Mathf.FloorToInt( 적 피해량 * 100f / (플레이어 방어력 + 100f) + 0.5f )
   - Mathf.RoundToInt는 은행가 반올림이라 쓰지 말 것
   - 곱셈을 먼저 할 것. (1 - def/(def+100)) 형태는 오차로 25가 24가 된다
   - 검증값: 30 → 25, 40 → 33, 150 → 125
   - HP 0이면 콘솔에 "패배" 출력 후 일시정지

아직 차단/방어는 만들지 마. 지금은 그냥 맞기만 하면 된다.
Unity에서 내가 해야 할 배치 작업도 순서대로 알려줘.
```

### ② 받은 뒤 내가 하는 일

| # | 작업 |
|---|---|
| 1 | EnemyData 에셋 3개 생성 — **각각 초록 / 주황 / 빨강** |
| 2 | 캐스팅 시간: 초록 2.5초 / 주황 3.0초 / 빨강 4.0초 |
| 3 | HP는 셋 다 300, 피해량은 초록 30 / 주황 40 / 빨강 150 |
| 4 | Capsule 3개를 화면에 나란히 배치 후 Enemy 컴포넌트 부착 |
| 5 | 캐스팅 바 UI 프리팹 만들어 적마다 연결 |
| 6 | 카메라 각도 조정 — **적 3체의 캐스팅 바가 한눈에 보이게** |

### ③ 확인 방법

- 세 캐스팅 바가 서로 다른 색으로 채워지는가
- 발동이 동시에 터지지 않는가 (0.5초 이상 벌어지는가)
- 빨강 한 방에 HP가 크게 깎이는가
- 아무것도 안 하면 약 몇 초 만에 죽는가 → **이 값을 기록해두세요.** 3단계 비교 기준입니다

### ④ 실측 결과 ✅

| 항목 | 결과 |
|---|---|
| 세 바 색 구분 | 초록 / 주황 / 빨강 정상 |
| 동시 발동 방지 | 같은 프레임 3건 → 1건만 통과. 0.49초 차단 / 0.51초 통과 |
| 빨강 한 방 | 실피해 **125** = 최대 HP의 25% |
| **무대응 전멸 시각** | **13.99초** — 총 10대 (초록 4 / 주황 3 / 빨강 3) |

발동 순서 `A2.5 B3.0 C4.0 A6.0 B7.0 C9.0 A9.5 B11.0 A13.0 C14.0`.
`C9.0`과 `A9.5`가 정확히 0.5초 간격으로 붙었고 지연 없이 통과했습니다.

> ⚠️ **위 13.99초는 구버전(적별 1색) 값입니다.** `04` M항(모든 적이 초록 보유 + 패턴 쿨다운)을
> 반영한 뒤 재측정한 값은 **17.01초**이며, 이쪽이 3단계 비교 기준입니다.
> 자세한 수치는 `10_결정사항_로그` §N10 참조.

---

## 3단계 — 차단 + 방어도 (1일, 핵심)

### ① 프롬프트

```
3단계. 여기가 이 프로토타입의 핵심이다.

침묵 코스트 2, 시전 0.2초. 입력 시 캐스팅 중이어야 사용 가능하고, 효과 적용 시에도 초록·주황 캐스팅 중이어야 성공한다. 먼저 발동하면 실패·경직 없음. 코스트·카드는 실패해도 소모된다.
모든 카드는 유효 입력 수락 즉시 같은 슬롯에 다음 카드가 들어온다. 효과는 시전 완료 시 적용하며, 기절 취소 시 추가 순환하지 않는다.

1. 차단(Interrupt) 카드: 캐스팅 중인 적을 대상으로 사용
   - 성공: 캐스팅 취소 + 해당 적 2.5초 경직
   - 초록·주황 캐스팅에 유효. 빨강에 쓰면 실패 (코스트는 소모, 로그 출력)
   - 캐스팅 중이 아닌 적에게는 사용 불가 (코스트 소모 없음, 실패음 대신 로그)
2. 방어도(Shield): Shield 카드 사용 시 플레이어에게 방어도 120 부여
   - 피해는 방어도에서 먼저 차감, 초과분만 HP에
   - 방어도는 전투 종료까지 유지 (시간 감소 없음)
   - 화면에 방어도 수치를 HP 옆에 표시
3. 타겟팅: 마우스 클릭 또는 좌우 방향키로 적 선택. 선택된 적 위에 조준 마커 표시
   - 전투 시작 시 1번 적 자동 지정. 타겟 사망 시 자동으로 다음 생존 적으로 이동
   - Deal/단일과 Interrupt는 타겟에게 적용. Deal/광역은 적 전체, 타겟 무관
   - 차단 카드는 타겟이 캐스팅 중이 아니면 슬롯 비활성 (입력 불가)
   - 타겟이 빨강 캐스팅 중이면 활성이되 사용 시 실패, 코스트는 소모
4. 적 사망 처리: HP 0이면 비활성화. 전원 사망 시 콘솔에 "승리"

카드 사용 시마다 콘솔에 [시간 / 카드명 / 코스트 / 대상 / 결과]를 출력해줘.
```

### ② 받은 뒤 내가 하는 일

| # | 작업 |
|---|---|
| 1 | 수정방패 카드의 방어도 부여량을 120으로 설정 |
| 2 | 침묵 카드가 Interrupt 분류인지 확인 |
| 3 | **최소 5회 이상 직접 플레이** |
| 4 | 플레이하면서 느낀 점을 `07_개발일지`에 메모 |

### ③ 적 방어력을 정할 때 확인할 것

3단계에서 적에게 방어력을 부여하면 플레이어 → 적 피해에도 소수가 생깁니다.

- 카드 8장의 `피해량 × 타수 × 100 / (적 방어력 + 100)`을 찍어보고, 크리티컬 1.8배 경우도 함께 확인
- **밸런스시트 `스킬` 시트의 기대 피해(소수)와 게임 내 실제 피해(정수)가 일치하는지** 대조
- 시트는 소수를 그대로 들고 있고 게임은 정수로 떨어뜨리므로, 오차가 크면 시트 쪽에 반올림을 반영할 것

### ④ 이때 관찰할 것 (중요)

플레이하면서 아래를 **의식적으로** 확인하세요. 나중에 회상하면 부정확해집니다.

- [ ] 적의 캐스팅 바를 실제로 보고 있는가, 아니면 내 손패만 보는가
- [ ] 주황이 뜨면 "침묵이 손에 있나"를 확인하게 되는가
- [ ] 침묵이 대기열에 있을 때 앞 카드를 써서 끌어오는 판단이 발생하는가 — 회차·시각·의도를 기록하고 4단계 결과와 함께 평가
- [ ] 코스트를 아끼려고 참는 순간이 있는가
- [ ] 빨강이 뜰 때 긴장되는가

---

## 4단계 — 계측 (반나절)

목적은 **적의 위협을 보고 공격·차단·방어에 코스트를 배분하는 판단이 발생하는지** 확인하는 것이다. 생존 시간 하나나 카드 사용 횟수만으로 성공·실패를 판정하지 않는다.

### ① 시작 전 확인 및 고정 조건

- 3단계 시험 결과를 `07_개발일지`에 통과 / 실패 / 미확인으로 기록한다. 기존 차단·손패 규칙 검증 기록을 활용하되, 방어도 흡수·기절 중 처리·승패 종료 처리까지 자동으로 검증 완료로 취급하지 않는다.
- 결과에 영향을 주는 실패가 있으면 먼저 수정하고 해당 항목을 재확인한다. 미확인 항목은 직접 확인하거나 이번 결과 해석의 제한으로 명시한다.
- **첫 캐스팅 시작 시점은 0초로 확정·확인 완료 (2026-09-17).** 전투 시작과 동시에 캐스팅을 시작한다 (`10` N11, `02` §9, `07` D-05). 이전의 2초 지연은 확정 전 초안값이므로 시험 조건으로 사용하지 않는다. 발동 후 경직은 초록 1초 / 주황 1.5초 / 빨강 2초, 차단 성공 경직은 2.5초다.
- 같은 빌드·덱 순서·적 구성·시작 HP·코스트·배속·포션 조건으로 비교한다. 시험 사이에는 전투 상태를 초기화한다. 같은 비교 묶음 안에서 수치를 바꾸지 않는다.
- 시험 조건과 결과는 `07_개발일지`에 기록한다. 아래 빈칸은 시험 전에 채운다.

| 조건 | 기록값 |
|---|---|
| 시험 날짜 / 빌드 또는 커밋 / 씬 | [ ] |
| 덱 8장과 등록 순서 | [ ] — §1단계 검증 덱과 일치 여부 확인 |
| 적 구성 / HP / 방어력 / 공격 리스트 | [ ] |
| 첫 캐스팅 시작 시점 / 발동 후 경직 / 최소 발동 간격 | [ ] |
| 플레이어 시작 HP / 방어력 / 크리티컬률·배율 | [ ] |
| 시작 코스트 / 상한 / 초당 회복 | [ ] |
| 배속 / 포션 허용 여부·시작 횟수·사용 원칙 | [ ] |
| 난수 조건 | [ ] — 시드 제어가 없으면 미고정으로 기록 |

### ② 코드 세션에 전달할 프롬프트

```text
4단계. 현재 전투 동작과 밸런스는 유지하고, 비교 시험에 필요한 계측을 추가해줘.
문서의 미결정 사양을 임의로 확정하거나 수치를 조절하지 마.

전투 시작 시 집계를 초기화하고, 승리 또는 패배가 확정될 때 최종 상태를 한 번만 기록해줘.
시간은 배속이 반영되는 게임 내 경과 초로 측정하고 일시정지 시간은 제외해줘.
종료 후 집계가 계속 증가하거나 결과가 다시 출력되지 않게 해줘.

전투 종료 요약:
- 승패 / 게임 내 전투 시간 / 남은 HP / 처치 수
- 카드별 사용 횟수
- 차단 시도 횟수 / 성공 횟수
- 방어 카드 사용 횟수 / 실제 방어도 흡수량 합계
- 코스트 부족으로 거절된 입력 횟수
- 코스트 상한을 초과해 버린 충전량 합계

카드 입력 수락마다 순서 번호 / 전투 경과 시간 / 슬롯 / 카드명 / 대상 /
입력 직전·직후 코스트를 기록해줘.
효과 적용·차단 실패·기절 취소 결과는 같은 순서 번호로 연결해줘.
시전 중 기절로 취소된 카드도 사용 횟수에 포함하되 결과는 취소로 구분해줘.

집계 정의:
- 카드 사용: 입력이 수락되어 코스트와 카드가 소모된 순간 1회. 효과 적용 시 중복 집계하지 않음.
- 차단 시도: 침묵 입력이 수락되면 1회. 빨강 대상과 시전 중 적 선발동에 의한 실패도 포함.
- 차단 성공: 효과 적용 시 실제로 적 캐스팅을 취소한 경우만 1회.
- 비캐스팅 대상에게 거절된 침묵 입력은 카드 사용·차단 시도에 포함하지 않음.
- 방어 사용: 방어 카드 입력 수락 시 1회. 기절 취소도 사용에는 포함.
- 방어 흡수: 방어도 부여량이 아니라 실제 피격으로 방어도에서 차감한 피해량의 합.
- 코스트 부족 입력: 전투 진행 중, 일시정지·기절·행동 잠금이 없고 대상 조건도 유효하지만
  코스트만 부족해서 거절된 카드 사용 입력(QWER 또는 손패 좌클릭)을 1회로 셈. 누르고 있는 프레임 수로 세지 않음. 입력 규칙은 10 A17 참조.
- 버린 코스트: 각 충전 시점에 상한을 초과한 양만 누적. 상한에 머문 시간과 구분.

'필요 카드를 끌어오기 위한 사용'을 코스트나 피해량으로 자동 추정하지 마.
카드 사용 순서·시간 로그를 근거로 플레이어가 의도를 별도로 메모할 수 있게 해줘.
콘솔 출력은 복사할 수 있는 텍스트로 제공하고, 별도 저장 시스템이나 분석 UI는 만들지 마.
완료 후 집계 확인 방법을 안내해줘.
```

### ③ 계측 자체의 확인

재미 비교 전에 짧은 확인 전투로 로그가 실제 동작과 맞는지 본다.

- 카드 한 번 사용 시 사용 횟수 1회. 효과 적용 때 다시 증가하지 않는지 확인.
- 성공한 침묵은 시도 1·성공 1, 빨강 대상 실패는 시도 1·성공 0인지 확인.
- 비캐스팅 대상의 거절 입력과 잠금 중 입력이 사용·차단 시도·코스트 부족 횟수에 섞이지 않는지 확인.
- 방어도 120 부여 자체로 흡수량이 증가하지 않고, 실제 피격분만 증가하는지 확인.
- 기절 취소는 사용 1회·결과 취소로 남고 카드 순환이나 집계가 중복되지 않는지 확인.
- 승패 후 요약이 한 번만 나오고, 다음 전투에서 집계가 초기화되는지 확인.

**상태: 위 6개 항목 플레이 모드 확인 완료 (2026-09-17).** `CombatMetrics` 구현·검증 기록은 `07` D-05 참조.
비교 시험(④)과 관찰 메모(⑤)는 아직 남아 있다.

> **상태: 비교 시험 완료 (2026-09-17, 11회차).** 회차별 결과·관찰·⑥ 해석은 모두 `07` D-05 에 있다.
> 무입력 1회 / 공격만 5회 / 공격+대응 5회. 승패를 합친 평균 전투 시간 17.01 → 22.21 → 41.04초, 승리는 대응 묶음에서만 2회.

### ④ 비교 시험 순서

| 묶음 | 방법 | 횟수·조건 |
|---|---|---|
| 무입력 | 카드·포션 사용 없음 | 기존 17.01초 기준 재현 확인. 차이가 나면 조건·로그를 확인하고 필요 시 반복 |
| 공격만 | 공격 카드만 사용 | 약 5회. 대응 카드는 사용하지 않음 |
| 공격 + 대응 | 위협에 맞춰 공격·차단·방어 사용 | 약 5회. 공격만 묶음과 같은 고정 조건 |

- 17.01초는 단일 표본이며 고정 전멸 시각이 아니다. 현재 빌드의 반복 측정 범위와 해석은 07 D-05를 참조한다. 새 시험에서 차이가 나면 빌드·공격 순서·측정 시간 기준부터 대조한다.
- 순서에 따른 학습 효과를 줄이려면 공격만 / 공격+대응 시험을 번갈아 진행할 수 있다. 실제 진행 순서를 기록한다.
- 대응 카드를 손패에 둔 공격만 시험은 사용 가능한 슬롯이 줄어들 수 있다. 이는 같은 덱에서 대응 카드 사용을 제한한 비교이며, 최적 공격 전용 덱과의 비교는 아니다.
- 중간에 버그 수정이나 수치 변경이 필요하면 변경 전·후 결과를 다른 묶음으로 분리한다.

### ⑤ 결과 기록 양식 — 07 개발일지에 복사

| 회차 / 묶음 | 승패 | 시간(게임 초) | 남은 HP | 처치 수 | 차단 시도/성공 | 방어 사용/흡수량 | 코스트 부족 입력 | 버린 코스트 | 로그 참조 |
|---|---|---|---|---|---|---|---|---|---|
| [ ] | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] | [ ] |

카드별 횟수와 사용 순서는 회차별 콘솔 로그를 함께 보관한다. 다음 관찰 메모는 해당 회차·로그 시각에 연결한다.

- 어떤 위협을 보고 공격을 멈추거나 코스트를 아꼈는가?
- 필요한 카드가 손패에 없었다면 어떤 카드를 먼저 썼는가? 목적이 피해인지, 카드 순환인지, 둘 다인지?
- 대응하지 못했다면 코스트 부족 / 카드 접근 불가 / 행동 잠금·기절 / UI 이해 부족 / 판단·입력 지연 중 무엇이 원인이었는가?
- 같은 조건의 반복이 아니라 위협·손패·코스트가 달라졌을 때 선택이 달라졌는가?

### ⑥ 결과 해석과 다음 결정

> **2026-09-18 후속 계측 완료:** 임시 순환 및 B/C/B+C 시험의 결과·미확인 사항·다음 판단은 07 D-05·D-06 참조. 정식 채택은 미결정.

> **상태: 해석 완료 (2026-09-17).** 결론은 **유지 + 추가 시험 1건**이며 **수치·사양 변경은 없다.**
> 전문은 `07` D-05 「4단계 ⑥ 결과 해석」. 미해결 쟁점 4건(다른 승리 전략 / 고코스트 카드의 낮은 사용률 /
> 끌어오기 방향 / 기절 페널티)도 거기에 있다.

| 관찰 | 함께 볼 항목 | 판단 원칙 |
|---|---|---|
| 생존 시간이 늘어남 | 승패·처치 수·남은 HP | 오래 버틴 것만으로 성공 판정하지 않음. 승리 시간과 패배까지 버틴 시간을 따로 비교 |
| 차단·방어 사용이 적음 | 사용 기회·코스트·카드 접근성·잠금·UI 이해·조기 사망 | 원인을 구분한 뒤 판단. 사용 횟수만으로 핵심 가설 실패라 단정하지 않음 |
| 사용 카드가 편중됨 | 피해 효율·적 위협·대응 필요성 | 카드별 횟수가 고르다고 좋은 것은 아님 |
| 카드 순서가 반복됨 | 위협·손패·코스트 조건 | 같은 상황의 같은 선택은 실패가 아님. 조건 변화에 따른 선택 변화를 확인 |
| 끌어오기 장면이 있음/없음 | 당시 의도·사용 순서·대응 기회 | 한 장면은 판단 발생의 근거이며 설계 전체의 성공 증명은 아님. 없으면 해당 상황이 있었는지부터 확인 |
| 코스트 부족·상한 손실이 많음 | 입력 원인·의도적 대기·실제 손실량 | 곧바로 회복량을 0.6 또는 1.0으로 바꾸지 않음. 원인을 기록하고 변경은 별도 결정 |

묶음별 승률과 회차별 결과를 함께 보고, 시간·HP·처치 수의 평균과 범위를 비교한다. 승리와 패배의 전투 시간은 구분한다. 약 5회씩의 시험은 원인을 찾기 위한 초기 비교이며 확정적인 통계 검증으로 취급하지 않는다.

관찰을 정리한 뒤 유지 / 추가 시험 / 한 가지 수치 변경 중 다음 행동을 정한다. 수치·사양을 바꾸기로 결정한 경우에만 결정 로그와 관련 문서를 함께 갱신한다.

### ⑦ 변경 이력

| 날짜 | 항목 | 변경 | 근거 |
|---|---|---|---|
| 2026-09-18 | 후속 계측 상태 | 구성별 시험 완료, 판단은 07 D-06 참조 | 10의 임시 시험 상태 참조. 사양 변경 없음 |
| 2026-09-18 | 후속 UI·입력 인계 | QWER 전용 표현·계측 용어를 정정하고 실제 입력 확인 항목 추가 | 10 A6·A17 및 정보확인 2단계 기록. 실제 마우스 검증 미완료 |
| 2026-09-17 | 1차 해석 정정 | 관찰과 가설을 구분하고 추가 시험은 한 조건 변경으로 제안 | 07 D-05 참조. 사양 변경 없음 |
| 2026-09-17 | 문서 정정 | 옛 덱 계산·재등장 횟수·방어 사용 횟수 단정 정정, 실측은 07 D-05 참조 | 10 결정 로그 및 기존 확정 규칙 참조. 새 사양 결정 없음 |
| 2026-09-17 | 4단계 계측·시험 절차 | 계수 기반 끌어오기 자동 추정 삭제. 사용·성공·흡수량 집계 정의, 고정 조건, 비교 시험·기록 양식 추가 | 폐지된 계수와 플레이 의도를 혼동하지 않고 동일 조건에서 대응 판단을 검증 |
| 2026-09-17 | 첫 캐스팅 시작 시점 안내 | 미결정·충돌 안내 → **0초 확정·확인 완료**로 갱신 | 10 N11·02 §9·07 D-05의 완료 기록 반영. 새 사양 변경 아님 |
| 2026-09-17 | 판정 기준 | 횟수·생존 시간만으로 성공/실패 확정 및 즉각 수치 조절 지시를 원인 분석으로 교체 | 처치·HP 보존·사용 기회를 함께 평가 |
| 2026-09-17 | 2단계 절차의 적 휴식 | 「발동 → 1초 휴식」 → **색별 경직(M8), 실제 발동 시점부터** | 일괄 1초는 구사양. 원본은 `10` 2026-09-17 절 |

---
## 막혔을 때 쓰는 프롬프트

### 컴파일 에러

```
아래 에러가 난다. 콘솔 로그 전문이다.

[에러 전문을 자르지 말고 그대로 붙여넣기]

관련 스크립트는 다음과 같다.
[해당 .cs 파일 전문]
```

> **에러 메시지를 요약하지 마세요.** AI는 스택 트레이스의 줄 번호와 타입명을 읽습니다.

### 동작이 이상할 때

```
의도한 동작: [무엇이 일어나야 하는지]
실제 동작: [무엇이 일어났는지]
재현 방법: [어떻게 하면 재현되는지]
관련 스크립트: [파일 전문]

원인 후보를 먼저 나열하고, 어떤 로그를 찍어 확인할지 알려줘. 바로 고치지 말고.
```

> "바로 고치지 말고"가 중요합니다. 원인 파악 없이 수정하면 다른 곳이 깨집니다.

### 수치를 바꾸고 싶을 때

```
[수치]를 [값]으로 바꾸고 싶다. 인스펙터에서 바꿀 수 있나?
없다면 [SerializeField]로 노출시켜줘.
```

> 이 요청이 자주 나온다면 애초에 하드코딩된 것입니다. 노출시켜두면 이후 밸런싱이 훨씬 빨라집니다.

### Unity 6에서 API나 메뉴가 다를 때

```
Unity 6.3 (6000.3.18f1)을 쓰고 있다.
알려준 [API명 / 메뉴 경로]가 내 버전에는 없거나 Obsolete 경고가 뜬다.

실제 화면: [본 것을 그대로 적기]

Unity 6 기준으로 맞는 방법을 알려줘.
```

> Unity 6 계열은 2022 LTS와 메뉴 구성·패키지 구조가 꽤 달라졌습니다. AI가 구버전 기준으로 답하는 경우가 흔하니, **버전을 명시하고 실제로 본 화면을 적어주는 것**이 가장 빠릅니다.

### 입력이 아예 안 먹을 때

```
QWER 키 입력이 전혀 반응하지 않는다.
Project Settings → Player → Active Input Handling은 [현재 설정값]이다.
[스크립트 전문]
```

> 십중팔구 Active Input Handling 문제입니다. `Both`인지 먼저 확인하세요.

---

## 매일 마무리 (5분)

| # | 할 일 |
|---|---|
| 1 | 프로젝트 폴더 백업 또는 Git 커밋 |
| 2 | 바뀐 수치를 **밸런스시트 엑셀**에 반영 |
| 3 | 사양이 바뀌었다면 **해당 기획서의 「변경 이력」** 에 기록 |
| 4 | **`07_개발일지`**에 한 줄 — 한 일 / 막힌 것 / 판단한 것 |

> 2~4를 빠뜨린 것이 1차 프로젝트의 P0(문서-구현 불일치)였습니다. 이번에는 그 기록 자체가 포트폴리오의 증거가 됩니다.

---

## 하지 말 것

- **아트·사운드·이펙트를 지금 붙이는 것.** 캡슐이 재미없으면 모델을 넣어도 재미없습니다
- **한 번에 여러 기능을 요청하는 것.** 버그 원인을 못 찾습니다
- **보상 3택1, 덱 편성 화면, 보스, 상태이상을 넣는 것.** 전부 4단계 이후입니다
- **"재미있게 만들어줘" 같은 요청.** AI가 임의로 정하면 내 게임이 아니게 됩니다


## 2026-09-16 차단·손패 규칙 변경 반영

> 사양 원본은 `10`의 「2026-09-16 차단·손패 규칙 변경 반영」 절에 있다.
> **상태: 구현 반영·실행 검증 완료 (2026-09-16).** 여기에는 확인 순서만 둔다.

### 새 사양 확인 순서

- 초록·주황 캐스팅 중 침묵 사용: 2코스트 소모, 입력 즉시 카드 교체, 효과 적용 시 성공하면 2.5초 경직. 주황 차단 후 초록, 초록 차단 시 초록 횟수 증가 없음 확인.
- 침묵 시전 중 대상 공격 발동: 적 공격 정상 적용, 경직 없음, 코스트·카드 소모.
- 빨강 대상: 차단 실패·경직 없음, 코스트·카드 소모. 비캐스팅 대상: 입력 거절·소모 없음.
- 불기둥 입력: 즉시 코스트·카드 소모, 같은 슬롯에 다음 카드 표시, 잠금 중 새 카드 입력 무시, 1.2초 뒤 효과.
- 기절로 불기둥 취소: 효과 없음, 코스트 반환 없음, 카드 추가 순환 없음.
- 씬·카드 에셋의 저장값을 통해 시험한다. 함수에 2.5를 직접 전달한 시험만으로 카드 사용 경로 검증을 대체하지 않는다.


