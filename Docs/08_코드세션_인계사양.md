# Scroll Hunter — 코드 세션 인계 사양

> **사용법:** 코드 세션을 새로 열 때마다 아래 「사양 블록」을 **맨 처음 한 번 붙여넣으세요.**
> 대화가 길어지면 초반 설정을 잊습니다. 세션이 바뀌면 다시 붙입니다.
> 수치의 원본은 **10_결정사항_로그.md**입니다. 이 파일은 코드 세션용 요약이며, 충돌 시 10의 최신 결정과 대조하세요.

---

## 사양 블록 (여기부터 복사)

```
# Scroll Hunter — 전투 공통 사양·제작 인계

## 현재 목표와 확정 제작 범위

2026-09-23 문서·엑셀·검사 기준 정정: 07#d09-doc-sync 참조. StartingDeckChecks 98개 통과, 게임 수치 유지.

2026-09-23 전체 패시브·포션 구현 완료: PS01~10 연결, PS10은 보상당 1% 제시, 포션 Shift 사용. 원본 10#all-passives-20260923(A28), 실행 09#passive-reward-checks, 변경·503개 검사·화면·새 창모드 빌드·한계는 07#d09-all-passives. PS01은 최대치만 증가하고 보충 없음. 새 빌드 실제 손 조작·자연 플레이는 미확인. 앞선 2종 빌드 계측과 분리. 후속 포션 회복량 변경은 10 A8·A28, 검사·최신 빌드는 07#d09-potion-30 참조.

2026-09-23 창모드 빌드 사용자 확인: UI 잘림 없음·배속/수동 정지·3전투 승리·재시작 초기화 확인. Player.log의 완주 2런을 07#d09-windowed-player-run에 보존·대조했으며, 순환→정밀 선택 답변은 2런째에 해당. 크리티컬 체감과 실제 비율은 구분. 후속 패배 후 첫 전투·HP·패시브·기본 능력치 초기화는 로그 확인(07#d09-defeat-restart). 순환 2스택 이월도 확인했으며 반복 밸런스는 미확인. 사양·수치·코드 변경 없음.

2026-09-23 창모드 재빌드 완료. 기본 실행 설정은 10#windowed-build-20260923(A27), 현재 실행 경로·방법은 09#windowed-build, 빌드 결과·미확인은 07#d09-windowed-build 참조. 전투 코드·씬·수치 유지. 빌드 당시 직접 조작·UI 배치는 미확인. 후속 사용자 확인은 위 완주 기록 참조.
2026-09-23 PS04·PS09 보상 연결·재빌드 완료. 원본 10#passive-rewards-20260923(A26), 실행 09#passive-reward-checks, 변경·434개 검사·화면·빌드·한계는 07#d09-passive-rewards. 패시브 추가 당시 빌드는 Builds/2026-09-23_Passives/ScrollHunter.exe. 당시 나머지 PS·포션은 미구현이었으며 현재는 A28 참조. 계승은 미구현. 이 2종 빌드의 사용자 확인은 후속 기록과 구분.
2026-09-23 최소 시작 화면·Windows 빌드 완료. 사양은 10#start-screen-20260923, 실행·배치는 09#start-screen-build, 변경·205개 함수/이벤트 검사·빌드 결과·한계는 07#d09-start-screen-build 참조. 실행 파일 직접 조작은 Escape로 중단되어 미확인. 앞선 사용자 보상 교체·스킵·재시작 확인은 07 D-09의 별도 계측이며 이 빌드 검증과 구분.
2026-09-22 시작 덱 8종 구현 완료. 현재 값·순서는 10_결정사항_로그.md#starting-deck-20260922, 실행은 09 시작 덱 절, 검사·남은 차이·한계는 07 D-08 참조. 개별 채널링 2종을 포함하며 보상·덱 교체·나머지 SK 구현 완료와 구분.
2026-09-22 스킬 문서 판정은 10_결정사항_로그.md#skill-review-20260922, 작성·검토 상태는 03·07 D-08 참조. 신규 카드 구현 완료와 구분.
전투 코어 1~4단계 구현·1차 계측 뒤, 사용자 승인으로 제작 일정을 확정했다.
현재 범위는 10_결정사항_로그.md#production-plan-20260923, 날짜별 할 일은 07_개발일지.md#schedule-20260921-23을 따른다.
스킬 풀 배정은 10_결정사항_로그.md#skill-pool-20260921, 작성 양식은 03 §2·§3을 따른다. SK별 작성 상태·기존 구현 카드와의 대응은 03 §3에서 관리하고 확인 근거는 07 D-07에서 읽는다. 작성된 제안값을 현재 구현값으로 자동 채택하지 않는다.
스킬 설계·우선 구현과 게임 진행 흐름을 병행한다. 승인된 진행 화면·보상 작업을 초기 프로토타입 보류 규칙으로 다시 막지 않는다.
현재 3전투 진행·HP 이월·재시작·적 배치는 구현 및 함수/화면 확인을 마쳤다. 사양은 10_결정사항_로그.md#three-battle-flow, 실행은 09_프로토타입_제작절차.md#three-battle-checks, 검증 한계·변경 파일은 07_개발일지.md#d07-three-battle-flow를 읽는다. 당시에는 보상·덱 교체가 미구현. 후속 구현은 아래 2026-09-22 기록 참조. 자연 플레이 완주·실제 클릭·빌드는 별도 확인.
반복 재미·임시 수치 정식 채택·미확인 기능이 모두 검증됐다는 뜻은 아니다.
큐브/캡슐과 Unity 기본 UI를 사용한다. 기본 코드 피드백은 이번 범위, 모델·After Effects VFX·사운드는 후속 범위다.

2026-09-22 보상·덱 교체 구현: 현재 시연의 규칙·신규 카드·패시브 확장 기반은 10_결정사항_로그.md#reward-flow-20260922, 실행은 09#reward-flow-checks, 변경·218개 검사·화면·한계는 07#d08-reward-flow 참조. 당시 실제 패시브는 미구현. 후속 전체 PS01~10·포션은 A28 참조. 자연 플레이 완주·손 조작·빌드는 미확인. 기존 시작 덱·적 HP 시험값 유지.

## 전투 구조
- 실시간. 플레이어 1인 vs 적 최대 3체
- 플레이어는 코스트만 충분하면 아무 때나 카드를 쓸 수 있다 (쿨타임 없음)
- 턴 개념 없음. 행동 게이지(ATB) 없음

## 자원 — 코스트
- 초당 0.8 충전
- 상한 10, 초과분 소멸
- 전투 시작 기본값 3. 충전·시작·상한·최대 HP·방어도 부여·차단 경직의 패시브 증가분은 10 A28 적용

## 최근 UI·입력 구현 인계 (2026-09-18)
- 카드 사용 입력은 10 A17, 일시정지는 10 A6 및 02 §10.2를 따른다.
- 플레이어 시전 표시는 07_개발일지.md#d06-ui, 카드·현재 적 공격 정보 및 정지·재개·좌클릭 사용은 07_개발일지.md#d06-info 참조.
- 기능 함수 호출 확인과 실제 마우스·키보드 경로 검증을 구분한다. 실제 입력은 07 D-07 제출 로그로 일부 확인됐고 모든 거절 조건은 미확인이다. 아래 1~4단계는 초기 제작 순서이며 최신 제작 일정은 위 참조를 따른다.

## 카드 — 덱과 손패
- 덱 8장(순서 있음) → 앞 4장이 손패(QWER) → 나머지 4장이 대기열
- 유효한 카드 입력 수락 즉시 그 카드는 덱 맨 아래로 이동, 대기열 맨 앞이 같은 슬롯에 진입
- 결정론적 순환. 셔플 없음. 등록 순서 그대로
- 손패 교체/버리기 없음 (4일차에 도입 여부 재검토)

## 행동 잠금 (GCD) — 중요
- 카드 사용 후 다음 카드를 쓸 수 없는 시간 = MAX(그 카드의 캐스팅 시간, 최소 GCD 0.4초)
- 즉 캐스팅기는 캐스팅 시간 전체가 잠금이고, 즉발기는 최소 GCD만 적용된다
- 개별 스킬 쿨타임은 없다. 카드 순환이 그 역할을 한다
- 캐스팅 중 다른 카드 입력은 무시한다 (버퍼 저장 없음)
- 캐스팅은 스스로 취소할 수 없다
- 캐스팅 중 기절당하면 캐스팅 취소, 코스트는 반환하지 않는다
- 포션(Shift)에는 행동 잠금이 적용되지 않는다
- 손패 슬롯 위에 남은 잠금 시간을 소수점 1자리로 표시한다

## 전투 진행
- 런 1회 = 전투 5회 (잡몹 4 + 보스 1). 선형 진행, 분기 없음
- 전투 종료 시 보상 1회 → 덱 리빌딩 → 다음 전투
- HP는 전투 사이에 이월된다. 방어도는 전투 종료 시 소멸한다

## 적 캐스팅 3색
- 초록(Green): 일반 공격. 대응 불필요
- 주황(Orange): 기절 공격. 차단 카드로 캐스팅 취소 가능
- 빨강(Red): 대형 피해. 방어도 카드로 흡수. 차단 불가
- 입력 시 대상이 캐스팅 중이면 사용 가능. 침묵 시전 0.2초 뒤에도 초록·주황 캐스팅 중이어야 성공하며, 그전에 발동하면 실패하고 경직 없음(코스트·카드는 소모)
- 적끼리 발동이 겹치지 않도록 최소 0.5초 간격 강제

## 적 공격 구성과 패턴 쿨다운 — 중요
- 모든 적이 초록(기본 공격)을 보유한다. 주황·빨강은 그 위에 추가되는 공격이다
- 보유 조합: A 초록 / B 초록+주황 / C 초록+빨강 / D 초록+주황+빨강 / 보스 전부
- EnemyData는 색 하나가 아니라 공격 리스트를 갖는다
- 패턴 쿨다운은 시간이 아니라 초록 사용 횟수로 센다
  주황: 사용 후 초록 1회 경과 / 빨강: 사용 후 초록 2회 경과 / 초록: 쿨다운 없음
- 우선순위 빨강 > 주황 > 초록
- 행동 선택 규칙 (순서대로 판정)
  1. 직전 행동이 주황이나 빨강이었다면 반드시 초록을 사용
  2. 쿨다운이 끝난 상위 공격 중 우선순위가 가장 높은 것 사용
  3. 없으면 초록 사용
- 발동 후 경직: 초록 1.0초 / 주황 1.5초 / 빨강 2.0초 / 차단 성공 시 2.5초
  경직 시간 = 발동부터 다음 캐스팅 시작까지의 간격
- 차단 성공 시 그 공격의 쿨다운도 소모된다 (한 번 쓴 것으로 친다)
  경직이 풀리면 반드시 초록이 먼저 온다: 주황(차단) → 초록 → 주황
  단 초록을 차단한 경우에는 초록 횟수를 올리지 않는다
- 주의: 플레이어 카드에는 쿨타임이 없다. 이것은 적 전용 규칙이다

## 타겟팅
- 타겟 개념은 하나뿐이다. 차단 전용 타겟이나 순환 키를 만들지 말 것
- 딜/단일과 차단은 타겟 1체, 딜/광역은 적 전체, 방어는 자신
- 타겟 조작: 마우스 클릭 또는 좌우 방향키
- 전투 시작 시 1번 적 자동 지정. 타겟이 죽으면 자동으로 다음 생존 적으로 이동
- 타겟 위에 조준 마커 표시
- 차단 카드는 타겟이 캐스팅 중이 아니면 슬롯 비활성 (입력 불가)
- 타겟이 빨강 캐스팅 중이면 활성이되 사용 시 실패하고 코스트를 소모한다

## 카드 효과 적용 시점 — 중요
- 일반 시전 효과는 완료 시 적용된다(0초는 즉시). 채널링은 진행 중 효과 유지/발생. 상세 원본: 10 A18, 제작 방법: 09 채널링 제작 절.
- 코스트 소모와 카드 순환은 발동 방식과 무관하게 입력 수락 즉시 1회다.
- 일반 시전의 타이머는 효과까지 = 캐스팅 시간 / 다음 입력까지 = MAX(캐스팅 시간, 최소 GCD). 채널링 시간 해석은 10 A18 참조.
- 캐스팅 중 기절당하면 취소된다. 코스트는 반환하지 않는다. 카드는 입력 수락 시 이미 순환했으므로 추가 순환하지 않는다
  일반 시전 효과는 미발생. 채널링은 유지 효과를 정리하며 이미 발생한 결과는 되돌리지 않는다(10 A18).
- 기절이 행동 잠금을 대체한다. 기절이 풀리면 곧바로 쓸 수 있다

## 손패 슬롯 비활성 표시 — 사유 4가지를 구분할 것
- 코스트 부족: 해당 슬롯만. 코스트 숫자를 빨갛게
- 타겟이 캐스팅 중 아님: 차단 카드만. 청회색 + 「잠김」 (10 A22)
- 행동 잠금 중: 손패 전체. 회색 + 남은 시간 숫자
- 기절 중: 손패 전체 보라 계열. 시간 표시 위치와 충전 음영 방식은 최신 10 A22를 따른다.
- 행동 잠금과 기절은 둘 다 손패 전체라 색으로 갈라야 한다.
  중앙 표시는 기절에만 붙인다. 색은 원인을, 중앙 표시는 상태를 알린다
- 기절·효과 지연은 구현 완료. 아래 2026-09-16 변경 사양도 **구현 반영·검증 완료**다

## 단일 / 광역 밸런스
- 손익분기 **적 2체.** 같은 코스트 기준 광역 피해량 = 단일 피해량 × 0.5
- 광역기의 대가는 코스트가 아니라 **캐스팅 시간** (0.8~1.2초)
- 광역기 코스트를 올리면 피해량도 비례해 올릴 것

## 대응 수단
- 차단(Interrupt): 캐스팅 중인 적 대상. 성공 시 캐스팅 취소 + **2.5초 경직**
- 방어도(Shield): 플레이어에게 부여. 피해는 방어도 → HP 순으로 차감

## 플레이어
- HP 500, 방어력 20, 크리티컬률 15%
- **공격력 스탯 없음.** 피해는 스킬에 적힌 피해량(절대값)으로만 결정된다
- SkillData에는 계수가 아니라 damage(int) 필드를 둘 것
- HP는 전투 사이에 회복되지 않는다 (소모성 자원)
- 회복 포션은 런 시작 기본 3/3. 사용·최대치·입력 거절 규칙은 10 A28
- 피해 공식: float raw = 피해량 * 타수 * 100f / (대상방어력 + 100f) * 크리배율
- damage는 int 필드, 계산 중간값은 float, 정수화는 최종 1회만
- 곱셈을 먼저 할 것. (1 - def/(def+100)) 형태는 부동소수점 오차로 25가 24가 된다
- 정수화: if (raw <= 0f) return 0; else return Mathf.Max(1, Mathf.FloorToInt(raw + 0.5f));
- Mathf.RoundToInt와 Math.Round는 둘 다 은행가 반올림(0.5→0, 2.5→2)이므로 쓰지 말 것
- 최소 피해 1. 단 피해량 0인 카드(방어·차단)는 0을 유지할 것
- 검증값 (플레이어 방어력 20): 30 → 25, 40 → 33, 150 → 125

## 확정 사항 (10_결정사항_로그 기준)
- 손패 슬롯 위치는 고정. 카드가 왼쪽으로 밀리지 않는다
- 캐스팅은 스스로 취소할 수 없다
- 캐스팅 중 다른 카드 입력은 버퍼에 저장하지 않고 무시한다

## 코딩 규칙 (반드시 지킬 것)
- 모든 수치는 [SerializeField] 또는 ScriptableObject로 인스펙터에 노출. 하드코딩 금지
- 스킬/적 데이터는 ScriptableObject로 분리
- **Unity 6.3 LTS (6000.3.18f1), C#, URP 기준**
- **입력은 구형 Input API(Input.GetKeyDown)를 사용한다.**
  프로젝트의 Active Input Handling이 "Both"로 설정되어 있다.
  Input System 패키지 방식으로 작성하지 말 것.
- API가 Unity 6에서 사용 중단(Obsolete)된 것이라면 대체 API를 알려줄 것
- 한 번에 한 기능씩. 요청하지 않은 기능(사운드, 파티클, 세이브 등)은 만들지 말 것
- 만든 뒤 "어떻게 실행해서 확인하는지"를 항상 함께 설명할 것
```

## (여기까지 복사)

---

## 단계별 요청 순서

세션 블록을 붙인 뒤, 아래를 **하나씩** 요청하세요. 한 번에 여러 개를 요청하면 버그 원인을 못 찾습니다.

### 1단계 — 코스트와 손패 순환 (적 없음)

```
1단계만 만들어줘. 적은 아직 없어도 된다.

1. CostSystem: 초당 0.8 충전, 상한 10, 시작 3. 현재값을 화면 좌하단에 숫자로 표시
2. SkillData (ScriptableObject): 이름, 코스트, 분류(딜/차단/방어), 피해량(int), 타수, 캐스팅 시간
3. DeckSystem: SkillData 8개 리스트를 받아 앞 4장을 손패로, 나머지를 대기열로 관리
   - QWER 키로 손패 사용. 코스트 부족 시 실패 처리 + 로그
   - 유효 입력 수락 즉시 사용한 카드는 덱 맨 아래로, 대기열 맨 앞이 같은 슬롯에 진입. 기절 취소 시 추가 순환 없음
4. 행동 잠금: 카드 사용 후 MAX(캐스팅 시간, 최소 GCD 0.4초) 동안 다른 카드 입력 무시
   - 최소 GCD(0.4)는 [SerializeField]로 노출
   - 잠금 중에는 손패 슬롯을 회색 처리하고 남은 시간을 소수점 1자리로 표시
5. UI: 손패 4칸에 카드 이름과 코스트 표시. 옆에 대기열 다음 1장을 회색으로 표시

테스트용 SkillData 8개를 만드는 방법과 확인 절차도 알려줘.
```

### 2단계 — 적과 캐스팅 바

```
2단계. 적을 추가한다.

1. EnemyData (ScriptableObject): 이름, HP, 방어력, 캐스팅 시간, 담당 색상, 피해량
   - 공격력 필드는 두지 말 것. 피해는 피해량으로만 결정된다
2. Enemy: 캐스팅 시간만큼 대기 → 발동 허가 후 실제 발동 → 해당 공격의 경직(초록 1.0초 / 주황 1.5초 / 빨강 2.0초) → 반복. 경직은 실제 발동부터 계산하며 별도 추가 대기 없음
3. 캐스팅 바 UI: 적 위에 진행률 바. 담당 색상으로 채워짐 (색은 인스펙터에서 지정)
4. 적 3체 배치. 적끼리 발동이 겹치지 않게 최소 0.5초 간격 강제
5. 플레이어 HP 500. 공격 발동 시 피해. 0이 되면 로그로 "패배"

아직 차단/방어는 만들지 마. 지금은 그냥 맞기만 하면 된다.
```

### 3단계 — 대응 수단 (핵심)

```
3단계. 여기가 이 프로토타입의 핵심이다.

침묵 코스트 2, 시전 0.2초. 입력 시 캐스팅 중이어야 사용 가능하고, 효과 적용 시에도 초록·주황 캐스팅 중이어야 성공한다. 먼저 발동하면 실패·경직 없음. 코스트·카드는 실패해도 소모된다.
모든 카드는 유효 입력 수락 즉시 같은 슬롯에 다음 카드가 들어온다. 효과 시점은 발동 방식별 10 A11·A18을 따르며, 기절 취소 시 추가 순환하지 않는다.

1. 차단 카드: 캐스팅 중인 적에게 사용 → 캐스팅 취소 + **2.5초 경직**
   - 초록·주황 캐스팅에 유효. 빨강에는 실패(로그)
   - 캐스팅 중이 아닌 적에게는 사용 불가 (코스트 소모 없음)
2. 방어도: 방어 카드 사용 시 플레이어에게 방어도 120 부여
   - 피해는 방어도 → HP 순 차감. 초과분만 HP에
   - 화면에 방어도 수치 표시
3. 타겟팅: 마우스 클릭으로 적 선택. 선택된 적에 표시

카드 사용 시마다 콘솔에 [시간/카드명/코스트/대상/결과]를 출력해줘.
```

### 4단계 — 검증 계측 ★

```
4단계. 재미 검증을 위한 계측을 넣어줘.

전투 종료 시 콘솔에 요약 출력:
- 총 전투 시간
- 카드별 사용 횟수
- 차단 시도 / 성공 횟수
- 방어 카드 사용 횟수
- 코스트 부족으로 실패한 입력 횟수
- 코스트가 상한에 도달해 소멸한 총량
```

---

## 4일차 판단 기준

**판정 절차는 `09_프로토타입_제작절차.md` 4단계 ④~⑥을 따른다.** 같은 빌드·덱·적 구성·시작 상태·배속·포션 조건에서 공격만 약 5회와 공격+대응 약 5회를 비교하고, 사용 순서와 플레이어의 의도를 함께 기록한다. 계측 구현 완료와 재미 검증 완료는 구분한다.

| 관찰 | 함께 볼 항목 | 판단 원칙 |
|---|---|---|
| 생존 시간이 늘어남 | 승패·처치 수·남은 HP | 오래 버틴 것만으로 성공 판정하지 않음. 승리 시간과 패배까지 버틴 시간을 따로 비교 |
| 차단·방어 사용이 적음 | 사용 기회·코스트·카드 접근성·잠금·UI 이해·조기 사망 | 원인을 구분한 뒤 판단. 사용 횟수만으로 핵심 가설 실패라 단정하지 않음 |
| 사용 카드가 편중됨 | 피해 효율·적 위협·대응 필요성 | 카드별 횟수가 고르다고 좋은 것은 아님 |
| 카드 순서가 반복됨 | 위협·손패·코스트 조건 | 같은 상황의 같은 선택은 실패가 아님. 조건 변화에 따른 선택 변화를 확인 |
| 끌어오기 장면이 있음/없음 | 당시 의도·사용 순서·대응 기회 | 한 장면은 판단 발생의 근거이며 설계 전체의 성공 증명은 아님. 없으면 해당 상황이 있었는지부터 확인 |
| 코스트 부족·상한 손실이 많음 | 입력 원인·의도적 대기·실제 손실량 | 곧바로 회복량을 0.6 또는 1.0으로 바꾸지 않음. 원인을 기록하고 변경은 별도 결정 |

묶음별 승률과 회차별 결과를 함께 보고, 시간·HP·처치 수의 평균과 범위를 비교한다. 승리와 패배의 전투 시간은 구분한다. 약 5회씩의 시험은 원인을 찾기 위한 초기 비교이며 확정적인 통계 검증으로 취급하지 않는다.

관찰을 정리한 뒤 유지 / 추가 시험 / 한 가지 수치 변경 중 다음 행동을 정한다. 수치·사양을 바꾸기로 결정한 경우에만 결정 로그와 관련 문서를 함께 갱신한다.

**기존 숫자의 해석:** 대응 사용 2회 이상, 고른 카드 사용, 전투 40±10초를 이번 비교의 일률적인 합격선으로 사용하지 않는다. 전투 시간은 기존 기획 목표와의 차이를 살펴보되, 승패·처치·HP·전투 구성을 함께 확인한 뒤 조정 여부를 결정한다.

### 판단 기준 정정 이력

| 날짜 | 항목 | 수정 전 | 수정 후 |
|---|---|---|---|
| 2026-09-23 | 전체 패시브·포션 인계 | 최신 범위·실행·검사 참조 | 10 A28·07 D-09·09 확인 절차 |
| 2026-09-23 | 패시브 구현 인계 | PS04·PS09와 현재 빌드 참조 | 10 A26·07 D-09·09 확인 절차 |
| 2026-09-22 | 보상 구현 인계 | 보상·덱 교체 미확인 안내 | 10 A23·07 D-08의 현재 범위·확장 기반·검사 참조 |
| 2026-09-22 | 시작 덱 구현 인계 | 초안·이전 덱 안내 | 10 A20·07 D-08의 현재 덱·코드·검사 참조. 미확인 항목 유지 |
| 2026-09-22 | 스킬 판정 인계 | 초안 상태 안내 | 10 스킬 검토 반영·07 D-08 참조. 구현 완료와 구분 |
| 2026-09-21 | 진행 구조 인계 | 확정 일정만 안내 | 10 3전투 진행·09 실행·07 구현/검증 참조 추가. 보상 구현과 구분 |
| 2026-09-21 | 제작 단계 인계 | 초기 전투 검증 중심 | 10 월~수 제작 일정·07 계획을 참조해 승인된 스킬·진행 기능 제작. 검증 한계 유지 |
| 2026-09-17 | 원본 안내 | 이 파일이 원본 | 10 결정 로그가 원본이며 이 파일은 요약 |
| 2026-09-17 | 대응 사용·카드 편중 | 횟수로 정상/이상 및 가설 실패 판단 | 사용 기회·상황·원인과 함께 해석 |
| 2026-09-17 | 코스트·전투 시간 | 부족하면 회복량 증가, 목표 시간 이탈 시 적 HP 조정 | 고정 조건으로 비교 후 유지/추가 시험/한 가지 수치 변경을 별도 결정 |
| 2026-09-17 | 판단 근거 | 단일 횟수·시간 중심 | 09 4단계 ⑥과 동기화. 승패·처치·HP·사용 순서·의도 함께 평가 |

---

## 코드 세션에서 하지 말 것

- 기획 판단을 코드 세션에서 하지 마세요. "이게 재미있을까요"는 이 세션에서 다룹니다
- 수치를 즉흥적으로 바꾸고 기록을 안 남기는 것 — 1차 프로젝트가 실패한 지점입니다
- 아트·사운드를 12일차 전에 붙이는 것

## 코드 세션에서 돌아온 뒤 할 일

1. 변경된 수치를 **밸런스시트 엑셀**에 반영
2. 사양이 바뀌었다면 **해당 기획서의 「변경 이력」 표**에 기록
3. **`07_개발일지`**에 그날의 판단 한 줄
4. 이 파일(인계 사양)의 수치도 갱신

> 1~4는 하루 5분입니다. 이걸 빠뜨린 것이 1차 프로젝트의 P0(문서-구현 불일치)였습니다.

---

## 이 세션(기획 세션)에 가져올 것

- 4단계 계측 로그 → 밸런스 판단
- "이 시스템이 재미없는 것 같다"는 느낌 → 원인 분석
- 기획서 초안 → 빈칸과 모호한 표현 점검
- 스킬 14종 / 적 5종 설계 → 역할 분화 검토
- 면접 대비, 역기획서, 포트폴리오 구성

## 2026-09-16 차단·손패 규칙 변경 반영

> 사양 원본은 `10`의 「2026-09-16 차단·손패 규칙 변경 반영」 절에 있다. 이 문서 본문(적 사양·3단계 대응 수단)에 이미 반영돼 있다.
> **상태: 구현 반영·실행 검증 완료 (2026-09-16).**
